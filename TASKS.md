# משימות פתוחות — סבב העיצוב

הקובץ הזה הוא רשימת העבודה החיה. `PROJECT.md` הוא יומן ההחלטות (מה כבר הוכרע ואומת), `TASKS.md` הוא מה שעוד לא נעשה. סוכן שממשיך מכאן: קרא קודם את `AGENTS.md`, ואז את הקובץ הזה. סמן `[x]` רק אחרי שנמדד בדפדפן, לא אחרי שנכתב.

**כל שלב נשלח בנפרד ובסדר הזה.** הסדר נבחר כך שאם העבודה נעצרת באמצע, המצגת במצב טוב יותר מכפי שהייתה, ולא באמצע ניתוח.

---

## המשוב שהוליד את הרשימה (2026-09-10)

בלשון המרצה, כי זה הקריטריון:

- ״עדיין יש משהו תבניתי מידי בעיצוב עצמו. שים לב שתמיד תמיד תמיד יש כותרת כזאת גדולה באמצע. אם יש שלישייה של דברים הם תמיד בסדר יורד בגדול באמצע. הקומפוזיציה נראית אותו דבר כל הדרך עד החלק האחרון של התמונות והסרטונים ופתאום זה מרגיש לי ממש הקלה.״
- ״אפשר להיות יצירתיים יותר עם הקומפוזיציה. עם גדלים של טקסט. עם הדגשות של טקסט. עם עושר הטקסט (אתה יכול להוסיף קצת טקסט בחלק מהשקפים מבחינתי זה מעולה רק לא להעמיס) גם בצבע וגם בBOLD וחד משמעית גם באפקטים מעניינים.״
- ״האפקט הזה ולא השתמשנו בו פעם אחת״ — ״ברק חולף״ מוטמע במערכת ואינו בשימוש באף שקף.
- ״אני לא סגור על כל עומס הצבעים הזה. אני אוהב את הכיוונים הכחולים, תכלת, אפרפר. לא יודע לגבי האדום ובורדו. אולי צריך להכניס יותר לבן ומאוד אהבתי את הטקסטים הצבעוניים האלה כמו בתמונה המצורפת שכתוב בה גדול.״
- רפרנס עיצובי נוסף להדגשות: <https://www.youtube.com/watch?v=75TEMepeV_8> (עיצוב בלבד, לא תוכן).

**מה שאני לא יכול לעשות מהסשן הזה:** לצפות בסרטוני היוטיוב. כל מה שנגזר מהם נגזר מהתיאור של המרצה ומהבריף הכתוב ב־`AGENTS.md`, ומאומת מול צילומי מסך.

---

## שלב 1 — שני באגים שנמדדו · קטן

- [x] **שקף ״מודל שפה גדול״ רץ הפוך.** ב־`src/content.json`, שקף 17, `items` הם `["גדול.", "שפה.", "מודל."]` בזמן שהכותרת אומרת ״מודל שפה גדול״. הרנדרר תקין — הסדר במסמך הוא ההפוך. להפוך את שלושת הפריטים יחד עם הכיתוב של כל אחד.
- [x] **מצב הצגה משאיר מסגרת למעלה ולמטה.** `.theater` שומר `padding: 0 0 68px` בשביל סרגל שכבר מוסתר, ו־`.stage` מחושב `calc((100svh - 68px) * 16 / 9)`. במצב הצגה שניהם צריכים לקחת את כל החלון: אפס padding ו־`min(100vw, 100svh * 16 / 9)`. פסי הלטרבוקס שנשארים במסך שאינו 16:9 הם בלתי נמנעים — הם צריכים להיות שחורים וסימטריים, כמו מאטה בקולנוע, לא מסגרת.

## שלב 2 — הלוגואים · בינוני

התיקייה הגיעה. 26 קבצים, 9 מהם PNG והשאר SVG.

- [x] **SVG לא נכנס למסמך, לעולם** — הוא יכול לשאת סקריפט, וזה כלל ב־`AGENTS.md`. את ה־SVG־בלבד צריך לרסטר בזמן הכנה (דפדפן מקומי → PNG), ורק ה־PNG נכנס פנימה כ־data URI.
- [x] סקריפט הכנה שממיר תיקיית לוגואים ל־PNG בגודל אחיד ובשקיפות, ומדווח משקל. לא חלק מ־`npm run build` — הרצה חד־פעמית שתוצריה נכנסים ל־`src/content.json`.
- [x] כל 26 הלוגואים הומרו ל־WebP וזמינים ב־`marks.json`. **בשימוש בפועל: שלושת הוורדמארקים בשקף הכלים, וסמל ChatGPT בשקפי הדמו.** Copilot, Word, Excel, PowerPoint, Outlook ו־Nano Banana ממתינים לשקף שיצדיק אותם.
- [ ] **איפה עוד הם מרוויחים את מקומם** — לא מיתוג קבוע: שקף הכתיבה יכול להחזיק את סמלי Word/Outlook לצד ChatGPT, שקף ״כלים · חיבורים · סקילים״ יכול להראות למה מתחברים, ושקף Nano Banana שייך לרצף התמונות. **לאשר מול המרצה איזה שקף מקבל מה לפני ההטמעה.**

## שלב 3 — הצבע · בינוני

הכיוון שהמרצה נתן: כחול, תכלת, אפרפר, יותר לבן; לא בטוח לגבי האדום והבורדו; והגרדיאנט של המילה ״גדול״ כרפרנס.

- [x] לגזור פלטה חדשה מהתמונה שצורפה — תכלת → כחול־לבנדר → סגול רך על כמעט־שחור. זו הפלטה של ההדגשות, לא של הרקע.
- [x] `danger` ירד מכל חמשת השקפים. ״נשבר כאן״ נאמר עכשיו בלי בורדו בכלל: פלטת `steel` — בלי שום גוון, לבן מלא על כמעט־שחור, הצבע פשוט מתנקז מהשקף. **הוחל על כל החמישה בלי להציג חלופה קודם, כי המרצה ביקש לא לעצור באמצע; אם הכיוון לא מדויק זה שדה אחד לכל שקף.**
- [x] יותר לבן: היום `--text` הוא כמעט־לבן וכל ההדגשות צבעוניות. כדאי ההפך — לבן מלא ככותרת, וצבע רק במילה אחת.

## שלב 4 — לשבור את התבנית · גדול, הלב של הסבב

זו התלונה המרכזית, וזו העבודה הכי גדולה. **לא לנסות לעשות את הכול בבת אחת.**

- [x] `statement` ו־`reveal` מרנדרים תמיד מרכז־במה. להוסיף לטבלת השדות בחירת **פריסה** (`layout`) — לפחות: מרכז, צמוד לימין למעלה, צמוד לשמאל למטה, ושתי עמודות. שורה בטבלה + בלוק CSS, בלי ענף ברנדרר. זה מה שנותן למרצה לגוון בעצמו.
- [x] שלישייה לא חייבת להיות רשימה יורדת במרכז. פריסה שנייה ל־`reveal`: שלוש עמודות, או מדרגות, או מילה ענקית עם השתיים האחרות קטנות בצד.
- [x] סקאלת טיפוגרפיה: היום כל כותרת מחושבת לאותו גודל יעד. שדה גודל לכותרת (S / M / L / ענק) כדי שיהיה קצב — שקף לוחש ואז שקף צועק.
- [x] לעבור על 35 השקפים ולתת לכל אחד פריסה שנבחרה, לא ברירת מחדל. **זה חייב להיעשות עם צילומי מסך, שקף־שקף.**

## שלב 5 — הדגשה ואפקטים · בינוני

- [x] **״ברק חולף״ אינו בשימוש באף שקף.** להשתמש בו במקומות שמרוויחים אותו — ככל הנראה כותרות הפתיחה של פרקים.
- [x] הדגשה בתוך משפט: היום `accent` היא מילה אחת שמקבלת גרדיאנט. צריך גם **הדגשה בתוך כיתוב** — מילה מודגשת, מילה בצבע — בלי לפתוח HTML חופשי לתוכן (סימון מוגבל שנשאר בוולידציה).
- [ ] המרצה אישר להוסיף מעט טקסט בחלק מהשקפים. **טרם נעשה בכוונה.** ההדגשות בסבב הזה סימנו מילים קיימות ולא הוסיפו אף מילה. תוספת טקסט היא החלטת תוכן — להציע ניסוח ולקבל אישור, לא לכתוב לו את ההרצאה.

## שלב 6 — סגירה

- [x] `npm run build` ו־`npm test` ירוקים.
- [x] הליכה מלאה על 35 השקפים קדימה ואחורה, בלי שגיאות קונסולה. עריכה במקום, ייצוא ושמירה נבדקו.
- [ ] **טרם נבדק:** מסך צר מול הפריסות החדשות, וכל סוגי השקפים בתוך העורך עצמו.
- [x] `PROJECT.md` מקבל רשומה, `README.md` מתעדכן אם דרך השימוש השתנתה, והרשימה הזאת מתעדכנת.

---

## מה כבר נסגר בסבב הזה (לא לחזור עליו)

- מערכת צבע במקום שבע פלטות מפוזרות, רקע ״רשת חיבורים״, סימן הכלי בשקפי הדמו — PR #15.
- השקף שקפץ הצידה לפני מעבר: `.entering` לא הוסר מהשקף היוצא — PR #15.
- שתי הנפשות הטקסט (״זוהר נודד״, ״ברק חולף״) — מוטמעות, PR #15. **״ברק חולף״ עדיין לא בשימוש בשום שקף — ראה שלב 5.**
- העותק בדפדפן שהסתיר כל פרסום: המסמך נושא `revision`, ומופיעה שורה שמציעה את הגרסה החדשה — PR #16.
