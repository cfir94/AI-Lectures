# המשך עבודה

עודכן ב־2026-09-11. לקרוא את `AGENTS.md` ואת `LECTURE-FLOW.md` לפני שינוי רצף ההרצאה.

## מצב נוכחי

- הרצף ״המדריך ל-2030״: 54 שקפים, מהם 37 מוצגים ו־17 מדולגים. כל השקפים המקוריים נשמרו.
- תמונת הפתיחה כבר הוטמעה. שלושה לוגואים מלאים, הצגת המרצה, המחשות LLM, דוגמאות כלי השיחה, מעבר Gemini/NotebookLM, סוכנים והשוואה עודכנו.
- הזכוכית נמצאת בשקף האישור האנושי, לא בשקף הכלים. יתר הלוגואים ב־`assets/tool-marks.json` זמינים כשיש צורך בתוכן.
- הרקעים כחול/סגול הובהרו ונוספו להם גרדיאנטים. גווני הטקסט שנדגמו נשמרו.

## ממתין למרצה

- [ ] **צילום הכתבה על סוכנים.** המרצה עדיין מחפש אותה. השקף `lecture-agents-work-article` שמור ומדולג; להטמיע, לציין מקור ותאריך ולבטל את הדילוג כשהיא מגיעה.
- [ ] **הנפשת הסוכן.** להעתיק את ״סוכן עובד מונפש.mp4״ מחבילת התמונות אל `dist/videos/agent-working.mp4` (בדיוק בשם הזה), ואז לבטל את הדילוג על השקף `lecture-agent-motion` דרך ״שקפים״. הוא כבר מוגדר להתנגן פעם אחת, בלי קול, בכניסה לשקף. עד אז הוא מדולג ולא מפריע.
- [ ] **תמונות שאין להן מקור טוב יותר.** ארבעת צילומי הכתבות הוטמעו מחדש מהמקורות ב־2400px. שלוש תמונות חידון התמונות הן 728px **גם במקור** שנשלח, ולכן אי אפשר לחדד אותן; ארבעת הפוסטרים של הסרטונים נשארים 960x540 כי הם חולצו מקובצי הווידאו. אם יש מקור גדול יותר לאחת מהן, אפשר להטמיע באותו תהליך.


- [ ] צילום הכתבה על השפעת סוכני AI על העבודה. להחליף את התמונה הריקה ב־`lecture-agents-work-article`, לעדכן מקור/תאריך ולהסיר דילוג. בינתיים לא להציג אותו.
- [ ] התאמת הפרומפטים והדוגמאות לקהל הספציפי וחזרה מלאה על ההדגמות בכלים האמיתיים.

## בדיקות והמשך טכני

- נבדקו 36 שקפים + שלושה סוגים נוספים בגרסת QA, כל 54 הצעדים קדימה ואחורה, טעינת תמונות וגבולות הבמה ב־390px; ללא גלישה או שגיאות קונסולה. צילום כל השקפים נבדק ברוחב 1440px.
- נבדקו הענן, קלט השלמת משפט, מעבר הווילון, שינוי כותרת וביטול בעורך, כפתור הורדת HTML וטעינה של HTML שיוצא באמצעות אותו מנגנון.
- [ ] להפוך את `tools/stage-audit.mjs` לנייד בין סביבות: כרגע הוא תלוי בנתיבי Playwright/Chromium של Linux, ולכן אינו רץ ב־Windows הזה. בוצעו בדיקות דפדפן דרך CUA במקום זאת. זה אינו דיווח על הצלחה של הסקריפט.
- [ ] בדיקת רגרסיה אוטומטית עם השוואת תמונות; בדיקת כל פקדי העריכה לכל סוג ומכשיר. הסבב הנוכחי בדק עריכה מייצגת, לא כל שילוב אפשרי.
- הסרטונים המקומיים לא נוגנו מחדש בסבב סידור התוכן הזה. הפוסטרים והנתיבים הקיימים נשמרו; לבצע חזרת הרצאה עם קבצי `videos` במחשב ההצגה.

## שמירה ופרסום

לבנות ולבדוק לפני קומיט; `dist/index.html` נשאר בגיט. כל push ל־main מפעיל GitHub Pages. יש לוודא שהגרסה החיה מציגה את השם החדש ואת הנתונים החדשים, ולא להסיק מגודל הקובץ. Sites הוא ערוץ נפרד; לא לפרסם אליו ולא לשנות את מזהה הפרויקט ללא צורך. כשקיים עותק ישן בדפדפן, לבחור ״טעינת הגרסה החדשה״; העותק הקודם נשמר כטיוטה.
