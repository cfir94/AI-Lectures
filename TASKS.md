# משימות פתוחות

רשימת העבודה החיה. `PROJECT.md` הוא יומן ההחלטות — מה כבר הוכרע, ולמה. הקובץ הזה הוא **מה שעוד לא נעשה**.

סוכן שממשיך מכאן: קרא `AGENTS.md` (הסכם העבודה והבריף העיצובי), אחר כך את הקובץ הזה. סמן `[x]` רק אחרי מדידה בדפדפן — לא אחרי שנכתב קוד.

---

## מצב נוכחי (2026-09-11)

המצגת חיה ב־<https://cfir94.github.io/AI-Lectures/> ומתעדכנת אוטומטית מכל דחיפה ל־`main`.

- 36 שקפים מוצגים; 11 שקפי הניסיון הראשונים שמורים במסמך ומסומנים ״מדולגים״.
- 31 בדיקות עוברות, `dist/index.html` מסונכרן עם המקור, ואין שגיאות קונסולה בהליכה מלאה קדימה ואחורה.
- מערכת העיצוב: **גוון סגור אחד, מהדגימה של המרצה** — סגול `#7a59bc` וכחול `#4378ab`. שש פלטות שנבדלות ב**ערך** ולא בגוון (`pearl` לבן · `prism` דיו · `ice` פחם · `cobalt` נייבי · `violet` חציל, שקף אחד · `steel` שחור), וארבע ערכות מצגת. אל תרוויח את הרמפה ואל תוסיף גוון שלישי — ראה `AGENTS.md`.
- **סמל כלי על שקף בהיר חייב להיות הגרסה השחורה.** הפלטה מצהירה `tone` והשקף כותב `data-tone`. יש בדיקה שנכשלת אם סמל לבן מגיע לשקף `pearl`.
- כל 35 השקפים מוצמדים לפלטה משלהם. המשמעות: **ערכת המצגת בפאנל ״עיצוב״ לא תשנה אותם** עד שילחצו על ״החזרת כל השקפים לערכת המצגת״. הפאנל אומר את זה במפורש.

---

## פתוח — ממתין להחלטת המרצה

- [ ] **שלוש התמונות של המרצה.** הוא הדביק אותן בצ׳אט כתמונות, ולכן הן הגיעו כתצוגה ולא כקבצים שאפשר להטמיע. **צריך שיעלה אותן כקבצים** (כמו שהעלה את תיקיית הלוגואים). היעדים שהוא כבר קבע:
  - **בועת טקסט עם יד רובוטית** ← שקף הפתיחה (״מצ׳אטבוט לסוכן״). הרקע שם נוקה ומחכה לה.
  - **בועת צ׳אט עם זרם חלקיקים** ← השקפים על צ׳אטבוט.
  - **שכבות עם צ׳יפים כחולים וסגולים** ← לפי שיקול דעת; מתאימה לשקף על איך המודל עובד.
  שלושתן בדיוק בגמוט של הדק (לבן, כחול, סגול), ולכן הן חומר אמיתי ולא איור — בדיוק מה ש־`AGENTS.md` מבקש.

- [ ] **שאר הלוגואים.** 26 סמלים מומרים ומוכנים ב־`assets/tool-marks.json` (WebP, גובה 180, שקוף). בשימוש בפועל: שלושת הוורדמארקים בשקף הכלים, וסמל ChatGPT בשקפי הדמו. **ממתינים לשקף שיצדיק אותם:** Copilot, Word, Excel, PowerPoint, Outlook, Nano Banana, ווורדמארקים מלאים נוספים. אל תפזר אותם — ב־`AGENTS.md` כתוב שסמל על שקף דמו הוא סימן מחלקה ולא מיתוג.
- [ ] **תוספת טקסט לשקפים.** המרצה אישר עקרונית וביקש להמתין. כשיחליט: **להציע ניסוח ולקבל אישור**, לא לכתוב לו את ההרצאה. `src/content.json` הוא התוכן שלו.
- [ ] **הזכוכית** מוצבת בשקף אחד (שלושת הכלים), שם היא ממסגרת את הוורדמארקים. להרחיב רק אם המרצה יבקש — ורק היכן שהיא מחזיקה תוכן, לא כקישוט בפינה.

## פתוח — עבודה טכנית

- [ ] **מסך צר מול הפריסות החדשות.** `corner`, `edge`, `low` ו־`wide` נבדקו רק ברוחב שולחני. צריך לבדוק ~400px ולוודא שהפריסות מתקפלות במקום לחתוך טקסט.
- [ ] **כל סוגי השקפים בתוך העורך** מול השדות החדשים (`layout`, `scale`, `arrangement`).
- [ ] **חבילת בדיקות דפדפן.** `tools/stage-audit.mjs` נכנס לריפו ותופס את הסוג שכואב: טקסט לא קריא, גרדיאנט שלא נצבע, תמונה שלא נטענה, מסגרת שלא תואמת לשקף, שגיאות קונסולה. **להריץ אותו אחרי כל `npm run build`.** מה שעוד חסר: השוואת צילומים בין גרסאות (רגרסיה ויזואלית של ממש), בדיקת העורך, ובדיקת מסך צר.

---

## עבודה שוטפת — מה שחוזר על עצמו ושווה לדעת

- **הענף `claude/clever-hypatia-m3bhhb` נמחק־ונבנה־מחדש מעל `origin/main` בכל סבב.** מיזוג ב־GitHub הוא squash, ולכן הענף מתפצל מהיסטוריית `main` מיד אחרי כל מיזוג. הדרך הנקייה: `git checkout -B <branch> origin/main` ואז להחיל מחדש את השינוי. אל תנסה למזג את `main` פנימה — זה מייצר קונפליקטים מלאכותיים בכל קובץ.
- **אימות פרסום לפי גודל הקובץ אינו אמין.** שינוי כמו `corner` ← `center` שוקל בדיוק אותו דבר, והבדיקה ״עוברת״ לפני שהאתר התעדכן. לקרוא את `#deck-data` מהאתר החי ולהשוות שדה אמיתי.
- **`dist/index.html` נשמר בגיט בכוונה** — כדי שאפשר יהיה לפתוח את הפרויקט בלי להתקין כלום. לבנות מחדש לפני כל קומיט.
- **`tools/rasterize-logos.mjs` אינו חלק מה־build.** הרצה חד־פעמית שממירה תיקיית לוגואים ל־`assets/tool-marks.json`. SVG לעולם לא נכנס למסמך.
