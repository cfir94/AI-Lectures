# תוכנית ויומן החלטות

## המטרה

״המדריך ל-2030: איך AI משנה את הדרך שבה אנו עובדים״. המבנה: יסודות וכלים, מצ׳אטבוט לסוכן, ומבט לעתיד עם רובוטיקה ומציאות מדומה. הדוגמאות יוחלפו לפי הקהל בלי לשנות את שפת העיצוב.

## מצב נוכחי — 2026-09-11

תמונת מצב לסוכן שמצטרף עכשיו. ההיסטוריה המלאה נמצאת בהמשך הקובץ, לפי סבבים.

**מה זה**: קובץ HTML אחד (`dist/index.html`, כ־2MB) שנבנה מ־`src`, עם תמונות ולוגואים מוטמעים. אפס תלויות ריצה ובנייה. הסרטונים המקומיים נשארים לצידו ב־`videos/` ונטענים רק בלחיצה. מתפרסם אוטומטית ל־GitHub Pages בכל דחיפה ל־`main`; פרטי הסבב הנוכחי להלן; סטטוס הפרסום מאומת לאחר הדחיפה.

**התוכן**: הרצאה מלאה בשלושה חלקים, 36 שקפים לקהל. 16 שקפים שמורים ומדולגים, כולל מקום הכתבה החסרה. השיוף החזותי והאימות מתועדים ב־`VISUAL-POLISH.md`.

**מנוע ההצגה**: שנים עשר סוגי שקפים, כולל ״רעיון והמחשה״ שמחליף טקסט ואסט יחד בכל צעד; ניווט מקלדת דו־כיווני, קפיצה לשקף, הערות מרצה, החשכה, טיימר, מסך מלא, ו**מצב הצגה** שמוריד מהמסך כל מה שאינו השקף.

**מנוע העריכה**: עריכה במקום של כל טקסט מוקרן; שכבת אובייקטים חופשית (טקסט, תמונה, צורה, רכיב חזותי) עם גרירה, שינוי גודל, הצמדה לגריד, שכבות וחיתוך תמונה; ביטול וביצוע חוזר עם כיווץ פעולות רציפות; טיוטות שמורות; ייצוא JSON ו־HTML נייד.

**שפת העיצוב**: מוגדרת כדרישה מחייבת בראש `AGENTS.md` — מינימליסטי, עתידני, הייטקי, תנועה עם משמעות, הדגשה שנגזרת מהתוכן, ושימוש בחומר אמיתי: צילומי מסך, לוגואים וסרטונים מהמחשב של המרצה.

**הגבול שנקבע**: כלי העיצוב לא גדל יותר. מכאן ההשקעה היא בתוכן. Figma ומעבר לפריימוורק נשקלו ונדחו — ההנמקה בסבב ״מצב הצגה, סרטונים ורכיבים נוספים״.

## סבב הפלואו החדש — 2026-09-11

- שולבו עדכוני origin/main עד `5da16b7` לפני העבודה; תיקוני צבע, לוגואים ומעברים נשמרו.
- התוכן מסודר לפי `LECTURE-FLOW.md`. הוטמעו שלוש תמונות מתיקיית המרצה, ללא מחיקת שקפים. מגבלת השקפים עלתה מ־48 ל־64 כדי להכיל את התוספות ואת השקפים השמורים; תקציב המסמך (2.4MB), גבולות טקסט ומספרי צעדים/דוגמאות נשארו כפי שהיו.
- נוספו שורות בטבלאות הכניסה/יציאה עבור `gather` ו־`curtain` ובלוקי CSS תואמים. הווילון משתמש ב־clip-path עם אטימות קבועה. במעבר cut הבמה החדשה ממתינה ליציאת האובייקט הקודם, בלי שקף שקוף או כותרות חופפות.
- `PALETTE_STYLES` נושא גם gradient אופציונלי; הוא נכתב לשקף ולמסגרת דרך אותו מסלול קיים. רק רקעי prism/cobalt/violet הובהרו. דגימות צבעי הטקסט נשמרו.
- הבנייה מפיקה HTML עצמאי בגודל כ־2MB. 38 בדיקות אוטומטיות עברו. בדיקות דפדפן: כל הסוגים ברצף QA, כל 54 הצעדים לשני הכיוונים ברוחב 390, תמונות וגבולות, סקירת תמונות ברוחב 1440, אינטראקציות שפה, וילון ללא פייד, עריכה/ביטול והורדת HTML. ללא שגיאות קונסולה שנצפו. כלי stage-audit עצמו חסום בנתיבי Linux שאינם קיימים; פירוט המשך ב־TASKS.

## גרסה ראשונה — 2026-09-10

- שני שקפים בלבד: פתיחה טיפוגרפית מינימלית, וניסוי צ׳אטבוט / סוכן לתכנון סדנה.
- שליטה של המרצה, ללא השתתפות קהל ברשת.
- שלוש ערכות צבע על אותה קומפוזיציה. טרם נבחרה פלטה סופית.
- עורך תוכן בתוך פריסה קבועה, עם שכפול דוגמאות, 2–6 שלבים ועד 20 דוגמאות.
- שמירה מקומית, ייצוא/ייבוא JSON והורדת HTML עצמאי עם התוכן הנוכחי.
- הדמיות מקומיות בלבד; ללא מודל AI, חשבונות, מסד נתונים או פעולות חיצוניות.

## החלטות טכניות

- HTML/CSS/JavaScript ללא ספריות ריצה. Node משמש רק לאריזה ולפיתוח.
- תוצר יחיד ב־`dist/index.html`, כולל כל העיצוב, הסמלים והקוד. אין גופנים או קבצים חיצוניים, fetch או service worker.
- עיצוב מבוסס משתני CSS, עם תמיכה ב־RTL, מקלדת ותנועה מופחתת. קשתות האור הן גאומטריה מופשטת ב־CSS, ללא תמונות או טעינות רשת.
- מעטפת התצוגה, מצב הניווט, נתוני התוכן והעורך מופרדים. סוגי השקפים הם נקודת ההרחבה.
- פורמט תוכן גרסה 2: `documentId`, `theme`, `selectedExampleId`, `slides`, `examples`. כל שקף כולל מזהה, סוג, שדות לפי הסוג והערת מרצה. כל דוגמה כוללת מזהה, שם, משימה, תשובה וצעדים עם תווית, תיאור ותוצר.
- ייבוא נבדק לפני החלפת התוכן. טקסטים מוצגים כטקסט ולא כ־HTML; JSON המוטמע ב־HTML עובר escaping.
- שמירת דפדפן מבודדת לפי מזהה החבילה המוטמעת. ייצוא HTML יוצר מזהה חדש. אין סנכרון בין מחשבים או בין האתר לקובץ המקומי.
- האתר המקוון משתמש באותו תוצר סטטי. גישת Sites פרטית לבעלים כברירת המחדל; פתיחת קהל רחב יותר היא החלטה נפרדת.

## קריטריוני קבלה

- קובץ HTML יחיד נפתח ללא התקנה או רשת, ומאפשר להציג ולערוך.
- מעבר קדימה/אחורה עובר בתשובה ובכל שלבי הסוכן; החלפת דוגמה מאפסת התקדמות.
- ניתן לשכפל דוגמה, לשנות טקסטים ולייצא תוכן לשימוש במחשב אחר.
- ייבוא תקין משחזר את התוכן; ייבוא שגוי משאיר אותו ללא שינוי.
- תצוגת 16:9 במסך רחב ופריסה אנכית במכשיר צר, ללא חפיפת טקסט ופקדים.
- מקלדת נגישה, מצבי מיקוד ברורים, מסך מלא ותמיכה בהעדפת הפחתת תנועה.

## תיקון כיוון בעקבות משוב — 2026-09-10

המשתמש הגדיר את העיצוב הראשון כיבש ועמוס וביקש להתבסס על [סיכום Google I/O 2026](https://www.youtube.com/watch?v=tfx2CjqtCUI), במיוחד 3:30–3:35 ו־4:57–5:10. נצפו פריימים ב־3:30, 3:35, 5:00, 5:05 ו־5:10: כותרת יחידה או מספר ענק, חלל ריק, והחלפת המסר במקום הצטברות אלמנטים.

- הוסרו רשת הכרטיסים, המיתוג הקבוע, המסגרות ופס השלבים.
- הפתיחה מציגה רק ״מצ׳אטבוט לסוכן״ ו״ממילים למעשים״.
- הניסוי מחליף מוקד יחיד בכל צעד: הצעה, מתכנן, מכין, בודק, מוכן. המטרה הקצרה נשארת כהקשר; תוצאת השלב מופיעה בשורה אחת.
- פירוט השלבים ומשפט המעבר נשמרים כהערות למרצה בעורך, ואינם מוקרנים. פורמט תוכן גרסה 1 נשאר תואם.
- פקדי ההצגה מוסתרים בכניסה וחוזרים בהזזת העכבר או ב־Tab. מצב מקומי וייצוא נשמרו.
- חבילת ברירת המחדל החדשה משתמשת במזהה חדש; עריכות מקומיות של הגרסה הקודמת אינן נמחקות או נדרסות.

## תוצאות האימות הראשוני

- שבע בדיקות אוטומטיות עברו: ייבוא/ייצוא, תוכן פגום, ניווט, איפוס, escaping, קובץ ללא משאבים חיצוניים וייצוא HTML שמכיל תוכן מעודכן וקוד תקין.
- בתצוגה המקומית נבדקו שני השקפים, כל שלבי הסוכן, החלפת מצב ודוגמה, שכפול ועריכה, שמירה לאחר רענון, דחיית ייבוא שגוי וייבוא תקין.
- נבדקו ערכות הצבע, תצוגה רחבה וצרה, כותרות ותוכן באורך המרבי, מקשי Page Down / Page Up והחצים, וכניסה ויציאה ממסך מלא.
- פתיחת `file://` נחסמה על ידי מדיניות כלי הדפדפן. לכן פתיחה בלחיצה כפולה על מחשב נוסף ושמירת קובץ ההורדה בפועל עדיין דורשות אימות ידני בדפדפן רגיל. הקובץ העצמאי והקוד שבתוכו נבדקו בנפרד.
- תנועה מופחתת וטיפול בכשל שמירה קיימים בקוד; לא בוצעה הדמיית הגדרות מערכת או חסימת אחסון דרך כלי הדפדפן.

## שלד חלק א׳ ומצגת מרובת שקפים — 2026-09-10

המשתמש הגדיר שזו המצגת שבה יעביר את ההרצאה בפועל, במקום PowerPoint, ושהשלד יישאר קבוע בין קהלים כשרק הדוגמאות מתחלפות. לכן שני השקפים הקבועים הוחלפו במערך שקפים.

- `slides` הוא מערך מסודר. כל שקף מצהיר על סוג, וסוג מוגדר פעם אחת ב־`SLIDE_TYPES` שב־`core.js`: השדות שלו, האורכים המרביים, רשימת פריטים אם יש, ומספר הצעדים. אותה טבלה מזינה את הבדיקה, את העורך ואת מקשי ההצגה.
- חמישה סוגים: משפט גדול, הדגמה חיה, מילים שנחשפות, חתיכות של שפה, וצ׳אטבוט מול סוכן.
- מצב הניווט הוא `{slide, step}`. רווח מתקדם צעד בתוך השקף ואז לשקף הבא; חזרה אחורה נוחתת על הצעד האחרון של השקף הקודם. מצבי הצ׳אטבוט והסוכן הם צעד 0 וצעדים 1 ומעלה באותו שקף.
- ההדגמות עצמן נעשות בכלים האמיתיים. שקף ״הדגמה חיה״ מחזיק את הבמה, ושומר את הפרומפט להעתקה בלחיצה. הפרומפט וההערות אינם מוקרנים.
- העורך נגזר מהטבלה: עריכת שדות, הזזת שקף, מחיקה והוספת שקף מכל סוג. מקטע הדוגמאות נשאר נפרד כי הוא משותף לניסוי.
- חבילת ברירת המחדל היא חלק א׳ של ההרצאה, אחד עשר שקפים ועשרים צעדים, לפי מהלך של עשר דקות: חיפוש מול ייצור, שלוש המשפחות, מסמך שנשמע אמיתי, פירוק ״מודל שפה גדול״, רשת החיבורים, חתיכות של שפה, המצאה בביטחון והמעבר לחלק השני.
- מזהה חבילה חדש, כדי שעריכות מקומיות של הגרסה הקודמת לא יידרסו ולא ייטענו בפורמט ישן.

### אימות

- עשר בדיקות אוטומטיות עוברות, ובהן מעבר על כל הצעדים של כל השקפים בשני הכיוונים, דחיית תוכן פגום לכל סוג שקף, ותקינות שקפים חדשים שנוצרים בעורך.
- נבדק בדפדפן על הקובץ העצמאי: כל עשרים הצעדים, חזרה אחורה, קפיצה בנקודות, העורך, עריכה חיה, הוספת שקף, כפתור העתקת הפרומפט, שלוש הערכות ותצוגה ברוחב 390 פיקסלים. אין שגיאות בקונסולה.
- העתקה ללוח נופלת חזרה לבחירת טקסט כשהדפדפן חוסם את ה־API על `file://`.

## שכבת תנועה ואלמנטים חזותיים — 2026-09-10

המשתמש הגדיר שהמצגת חזרתית מדי: אותה קומפוזיציה, אותה כניסה ואותו רקע בכל שקף. הבקשה הייתה יכולת לבחור אנימציות ואלמנטים, לא סט קבוע אחר.

- שלוש טבלאות ב־`core.js` — `MOTIONS`, `BACKDROPS`, `TRANSITIONS` — במבנה של מפתח מול שם בעברית. הן מזינות את הבדיקה, את התפריטים בעורך ואת ה־CSS, בדיוק כמו `SLIDE_TYPES`.
- `motion`,‏ `backdrop` ו־`transition` הם שדות משותפים לכל סוגי השקפים ונדחפים לכל טיפוס בלולאה אחת. מסמך ישן בלי מעבר פר שקף מקבל את המעבר הגלובלי שלו כברירת מחדל.
- שדה מסוג בחירה שחסר בקובץ מיובא מקבל את ברירת המחדל; ערך שקיים ואינו חוקי פוסל את הקובץ. כך קובץ ישן נטען, וקובץ שבור לא עובר בשקט.
- שני סוגי שקפים חדשים: מספר ענק שמטפס אל ערכו, ושניים זה מול זה שמדליק צד בכל צעד.
- הפרדה בין שתי שכבות תנועה: כניסת שקף חיה על `.scene > *` ומונעת על ידי מחלקת ה־motion, שנשלפת ברינדור של צעד בלבד; אנימציית צעד חיה על אלמנטים פנימיים ורצה גם כשהשקף כבר על הבמה.
- מעבר בין שקפים מחזיק שני שקפים ב־DOM לרגע. השקף היוצא מוסר כשהאנימציה שלו עצמו נגמרת — `animationend` עולה במעלה העץ, ולכן נבדק `event.target` — ובכל מקרה אחרי 900 מילישניות.
- כל לולאת רקע תוכננה כך שאתחול מחדש ברינדור אינו נראה: תזוזות קטנות, פעימות שקיפות ורשת שמתקדמת בדיוק תא אחד.

### אימות

- שתים עשרה בדיקות אוטומטיות עוברות, ובהן נפילה לברירת מחדל של פריסט חסר, פסילה של פריסט שגוי, ומספר הצעדים של כל סוגי השקפים.
- נבדק בדפדפן: כל הרקעים, כל התנועות, שלושת המעברים כולל ספירת השקפים ב־DOM תוך כדי ובסוף, בניית שקף מספר ושקף מפוצל דרך העורך, ספירת המספר, ותצוגה ברוחב 390 פיקסלים. אין שגיאות בקונסולה.
- שלושה באגים תוקנו תוך כדי: שקף שנוסף נפתח מקופל בעורך, אירוע `input` של תפריט בחירה נכנס למסלול של שדות טקסט, ורווח לאחר לחיצה על נקודת ניווט הפעיל את הנקודה במקום להתקדם.

## תיקון מעברים ותמונות — 2026-09-10

המשתמש דיווח שהמעברים אינם חלקים ורואים טקסט על טקסט, וביקש יכולת להעלות תמונות.

- הסיבה לחפיפה: `.slide` היה שקוף, ולכן בהצלבה שני השקפים נראו יחד ושתי הכותרות הצטלבו במרכז. עכשיו לשקף יש רקע אטום, השקף היוצא יושב מעל ומבצע לבדו את ההצלבה, והשקף הנכנס אינו דוהה מתחתיו. נוסף `--enter-delay` שמחזיק את תוכן השקף הנכנס עד שהיוצא התפנה. אומת בדפדפן: בכל רגע במעבר נראית כותרת אחת בלבד.
- סוג שקף חדש **תמונה**, בשני מצבים: ממלאת את הבמה עם הצללה וכותרת מעליה, או נכנסת בשלמותה עם טקסט מתחתיה.
- תמונות נשמרות כ־data URI בתוך התוכן, כדי שהקובץ העצמאי יישאר עצמאי. ההעלאה מכווצת דרך canvas עד 1920 פיקסלים ל־WebP באיכות 0.82, עם נפילה ל־JPEG. תמונת בדיקה של 175KB ירדה ל־10KB.
- הבדיקה מקבלת רק data URI של תמונה רסטרית. SVG נדחה במפורש כי הוא יכול לשאת סקריפט, ואין בו צורך כאן. ערך ריק מותר ומצייר מסגרת מקווקוות על הבמה.
- מגבלת הייבוא עלתה מ־500KB ל־12MB, כי התוכן כולל עכשיו תמונות.

## עוד פלטות, רקעים ועזרים למרצה — 2026-09-10

- ארבע ערכות צבע נוספות: יער ומנטה, פחם וענבר, דיו ונייר, סגול ולילך. סך הכול שבע.
- `THEMES` הפך ממערך לטבלה של מפתח מול שם, תיאור ושני צבעי תצוגה. כפתורי הערכות נוצרים מהטבלה במקום להיות כתובים ב־HTML, כך שערכה חדשה היא שורה אחת ובלוק משתני CSS. הבדיקה עברה מ־`includes` ל־`Object.hasOwn`, כדי שמפתח שיורש מהאב־טיפוס לא ייחשב חוקי.
- שלושה רקעים נוספים: קרני אור, גלים, הילה. גם הם בנויים כך שאתחול מחדש אינו נראה.
- סוג שקף **טיימר**: ספירה לאחור שהמרצה מפעיל מהשקף. מצב הטיימר יושב מחוץ לחבילת התוכן, במפה לפי מזהה שקף, ולכן רינדור מחדש — החלפת ערכה, פתיחת הערות — אינו מאפס אותו.
- שני קיצורים למרצה: `N` להערת המרצה על הבמה, `B` להחשכה. שניהם מצב תצוגה בלבד ואינם נשמרים בתוכן.
- שדה מספרי אמיתי (`digits`) נוסף למפרט השדות, כדי שדקות הטיימר לא יקבלו טקסט חופשי.

### אימות

- חמש עשרה בדיקות אוטומטיות עוברות, ובהן כל שבע הערכות, דחיית מפתח ערכה שיורש מהאב־טיפוס, ודקות טיימר שאינן מספר שלם.
- נבדק בדפדפן: שבע הערכות מייצרות שבעה צבעי הדגשה שונים, שלושת הרקעים החדשים מצוירים ונראים, הטיימר סופר, נעצר באמת בהשהיה ומתאפס, `N` ו־`B` עובדים, ורווח ממשיך להתקדם אחרי שימוש בכפתורי הטיימר.
- באג שתוקן: `blur` על כפתור הטיימר לא תפס, כי `render` משחזר פוקוס לפי `data-action` והרינדור קרה לפניו. הבלר הוקדם.

## גילוי התמונות — 2026-09-10

המשתמש לא מצא איפה מעלים תמונה. בצדק: היכולת הייתה קיימת רק בשקף מסוג תמונה, וכדי להגיע אליה היה צריך לגלול מתחת לאחד עשר מקטעי שקפים ולמצוא תפריט בתחתית העורך.

- כפתור הוספת השקף עלה לראש העורך, במקום שבו מחפשים ״הוסף״.
- **תמונה משלך** נוספה כאפשרות של רקע הבמה, ולכן כל שקף יכול לשאת תמונה מאחורי הטקסט. שדה ההעלאה נפתח בתוך אותו שקף ברגע שבוחרים את הרקע הזה, כך שהדרך מהמחשבה לתוצאה היא שתי לחיצות.
- ההצללה מעל תמונת רקע נבנית מ־`color-mix` על צבע המשטח, ולא משחור קבוע, כדי שגם ערכה בהירה תישאר קריאה.
- באג שנתפס בבדיקה: שינוי בתפריט בחירה רינדר מחדש את הבמה אך לא את העורך, ולכן שדה התמונה לא הופיע עד לפתיחה מחדש. שינוי של שדה רקע מרנדר עכשיו גם את העורך.

## שכבת אובייקטים חופשית — 2026-09-10

המערכת עדיין בשלבי בניית כלי העריכה, והתוכן הסופי של ההרצאה יגיע אחר כך. כדי לא להפוך כל שקף לעורך פריסה נפרד ולא לשבור את הסוגים הקיימים, נוספה לכל שקף שכבת אובייקטים חופשית מעל הסצנה המובנית. נוסף גם סוג שקף **במה חופשית** למי שרוצה לבנות קומפוזיציה מאפס.

- `objects` הוא מערך אופציונלי בכל שקף ונשמר בפורמט תוכן גרסה 2. מסמכים ישנים ללא המערך מקבלים מערך ריק בזמן הבדיקה, כך שהתאימות נשמרת.
- ארבעה סוגי אובייקטים: טקסט, תמונה, צורה ורכיב חזותי. לכל אובייקט מזהה, מיקום ומידות באחוזים מהבמה, סיבוב, שקיפות וסדר שכבה לפי מיקומו במערך.
- טקסט חופשי מקבל תוכן, גודל, משקל, יישור, צבע ומראה: צבע אחיד, הספקטרום של ערכת הצבעים או קו מתאר. בחירת המראה מופרדת מתנועת הכניסה של השקף.
- תמונה חופשית משתמשת באותו צינור בטוח של תמונות מוטמעות: כיווץ בדפדפן, data URI רסטרי בלבד, ללא SVG וללא משאב רשת. אפשר לבחור contain או cover ועיגול פינות.
- שבע גרפיקות וקטוריות מוטמעות ללא נכסים חיצוניים: מלבן, עיגול, קו, חץ, כוכב, כתם וטבעת. צבע המילוי, צבע הקו ועוביו ניתנים לעריכה.
- במסך רחב העורך נפתח לצד הבמה ולא כחלונית שחוסמת אותה. לחיצה על אובייקט בוחרת אותו, גרירה מזיזה אותו, וידית הפינה משנה את גודלו. שדות מספריים מאפשרים עריכה מדויקת, סיבוב ושקיפות.
- כל האובייקטים עוברים ולידציה מלאה לפני שמירה או ייבוא: סוגים מוכרים בלבד, צבע hex, data URI בטוח, גבולות מספריים ומיקום שנשאר בתוך הבמה. המגבלה היא 40 אובייקטים לשקף ו־2.4MB לחבילת התוכן כולה. מזהי אובייקטים ייחודיים בכל המסמך, לא רק בתוך שקף.
- במסך צר נוסף מצב **עריכת במה** שמכווץ את העורך לרצועה תחתונה ומשאיר את הבמה זמינה לגרירה ולשינוי גודל. אותו כפתור מחזיר את פאנל השדות המלא.
- ייבוא בודק גם שה־Base64 בנוי נכון, שהחתימה תואמת לפורמט המוצהר ושהדפדפן מצליח לפענח כל תמונה בפועל לפני החלפת התוכן הפעיל.
- הוחמרה גם בדיקת פריסטים קיימים מ־`in` ל־`Object.hasOwn`, כדי שמפתחות בירושה כמו `constructor` לא יעברו כערך חוקי.

### אימות

- שבע עשרה בדיקות אוטומטיות עוברות אחרי הוספת בדיקות לכל סוגי השקפים, גבולות מיקום וגודל, צבעים, מזהים כפולים בין שקפים, מפתחות prototype ומגבלת הגודל הכוללת.
- נבדק בדפדפן: הוספה ועריכה של טקסט צבעוני, חץ ותמונה חופשית; כיווץ התמונה ל־WebP; גרירה; שינוי גודל; שכפול; מחיקה; סדר שכבות; שקף במה חופשית; וייצוא HTML שמכיל את האובייקטים והתמונה.
- ב־Chromium ברוחב 390 פיקסלים מצב עריכת הבמה משאיר את הבמה גלויה, הגרירה עובדת ואין גלילה אופקית. ייבוא תמונה בעלת חתימה תקינה אך תוכן פגום נדחה בלי להחליף את המצגת הפעילה.

## עריכה ישירה ורכיבים חזותיים — 2026-09-10

- טקסט חופשי נערך בלחיצה כפולה ישירות על הבמה. סרגל צף לאובייקט הנבחר מציג צבע, כניסה, יציאה ומצב הצמדה בלי לחפש את הכרטיס בעורך.
- גרירה ושינוי גודל נצמדים לרשת של 5% ולקצוות ולמרכזים של אובייקטים אחרים, עם קווי הנחיה בזמן הפעולה. אפשר לבטל הצמדה פר אובייקט; החצים מזיזים אובייקט נבחר לאורך הרשת.
- טקסט מובנה יכול להפוך לאובייקט מקושר באמצעות `bind`. הערך נשאר יחיד ומסונכרן בשני הכיוונים, והרינדור המובנה מוסתר כל עוד התיבה קיימת. מחיקת הקישור מחזירה את פריסת התבנית.
- לכל אובייקט יש פריסטים נפרדים של כניסה ויציאה. יציאת אובייקט יכולה לרוץ גם כשהמעבר הבא הוא חיתוך; העדפת הפחתת תנועה עדיין מבטלת את כולן.
- נוספה טבלת `VISUALS` עם אקורדיון אינטראקטיבי, זכוכית נוזלית ושדה כוכבים ממוקם דטרמיניסטית. ההשראה שסופקה הוטמעה מחדש ללא React, styled-components, כתובת תמונה חיצונית או מפת displacement כבדה.
- מעבר בין שקפים עבר מהגדרה גלובלית לשדה בכל שקף. שדה המעבר הגלובלי נשמר רק כתאימות למסמכים קיימים.
- שמונה עשרה בדיקות אוטומטיות עוברות. בדפדפן אומתו עריכת טקסט ישירה וסנכרון, צבע, הצמדה, שלושת הרכיבים, כניסה ויציאה, מעבר פר שקף וניווט מלא; ב־390×844 אומתו מצב עריכת במה, היעדר גלילה אופקית וסרגל בתוך המסך. אין שגיאות קונסולה.

## תיקון עריכת הטקסט על הבמה — 2026-09-10

המשתמש דיווח שלחיצה כפולה לא פותחת רוב הטקסטים לעריכה, שהתיבה קופצת ומשנה גודל, ושאין אופציה אמיתית לטקסט צבעוני. מדידה בדפדפן אישרה את שלושתם, וחשפה באג רביעי שאיש לא דיווח עליו.

- **רק חלק מהטקסטים היו ניתנים לעריכה.** `data-slide-text` נפלט רק לחמישה שדות ברמת השקף. בשקף ״מילים שנחשפות״ רק אחד מתוך חמישה טקסטים הגיב, ובשקף הניסוי אף אחד. עכשיו כל מחרוזת שמוקרנת נושאת את הנתיב שמחזיק אותה, כולל נתיבים מקוננים כמו `items.0.word` ו־`sides.1.heading`, וגם הטקסטים שמגיעים מהדוגמה.
- **הלחיצה הכפולה הזיזה את הטקסט.** היא המירה את השדה לאובייקט חופשי לפי פריסה קבועה, והכותרת קפצה מגופן 105 ל־54 ונדדה במקום. עריכה מתרחשת עכשיו במקום, על האלמנט עצמו; ההמרה לאובייקט נשארה פעולה נפרדת ומפורשת.
- **טקסט צבעוני.** `textStyle` הצטרף לשדות המשותפים, ולכן כל שקף בוחר בין צבע אחיד, צבעוני וקו מתאר. במקביל, בחירת צבע לאובייקט טקסט הפסיקה לאפס בשקט את הסגנון ל״צבע אחיד״.
- **איבוד עבודה בשקט.** אובייקט טקסט שקשור לשדה בשקף קיבל מגבלה של 500 תווים, בעוד הכותרת מוגבלת ל־40. הקלדה של 41 תווים הפילה את הבדיקה, `save` בלע את החריגה והציג ״השמירה במכשיר אינה זמינה״ — הודעה שגויה, כי האחסון היה תקין. אחרי רענון כל ההקלדה נעלמה. עכשיו `textLimit` גוזר את המגבלה מהשדה הקשור, ו־`save` מפריד בין תוכן לא תקין לבין אחסון חסום ואומר את האמת על כל אחד מהם.
- **שני באגים נוספים שנתפסו תוך כדי:** בנייה מחדש של הבמה מתוך אירוע blur זרקה `NotFoundError` וקטעה את הרינדור; היא נדחית עכשיו לפריים הבא. וכלל ה־CSS `h1 span` צבע כל span בכותרת, כך שתנועת ״מילה אחרי מילה״ הפכה כותרת שלמה לצבעונית בלי שביקשו — הגרדיאנט שייך עכשיו למחלקה של מילת ההדגשה בלבד.
- שדות הדוגמה רוכזו ל־`EXAMPLE_FIELDS` ו־`EXAMPLE_STEP_FIELDS`, במקום להיות כתובים בתוך הבדיקה. אובייקט חדש נוחת בהיסט קטן מקודמו, כדי שלא ייעלם מתחתיו.

### אימות

- עשרים ואחת בדיקות אוטומטיות עוברות, ובהן שלוש חדשות: מגבלת האורך של טקסט קשור, פענוח נתיב מוקרן לשדה שמחזיק אותו, ובחירת מראה הטקסט.
- נבדק בדפדפן: כל הטקסטים בכל אחד עשר השקפים, עריכה במקום שאינה מזיזה את הכותרת (105px לפני ואחרי), יציאה בלחיצה בחוץ, אכיפת ארבעים התווים, טקסט צבעוני, גרירה, שכבות, וייצוא נייד. אין שגיאות בקונסולה.
- לא הצלחתי לשחזר את התקיעוּת במצב עריכה שדווחה. מסלולי היציאה חוזקו וה־`NotFoundError` שירה בדיוק שם תוקן; אם זה חוזר, צריך את הצעדים המדויקים.

## מצב עריכה, סרגל צף וקפיצה לשקף — 2026-09-10

המשתמש דיווח שוב שלחיצה כפולה לא עובדת, ושה־UI של העריכה לא נוח. מדידה הראתה את השורש: העריכה על הבמה הייתה מותנית בכך ש־`#editor` פתוח. מי שהסתכל על הבמה ולחץ פעמיים לא קיבל כלום, בלי שום סימן שחסר תנאי.

- **מצב עריכה הפך למתג משלו.** `editing` הוא מצב מפורש, הכפתור בסרגל מציג `aria-pressed`, וכל בדיקות `$("#editor").open` שהיו פזורות בקוד הוחלפו בו. פתיחת סרגל הצד וסגירתו אינן משנות אותו יותר.
- **סרגל צף על הבמה** (`#stage-tools`) עוסק בשקף שמולך: ארבעת סוגי האובייקטים, מראה הטקסט, מעבר לרשימת השקפים ויציאה. סרגל הצד נשאר למבט־על ולסידור, כפי שהתבקש.
- **קפיצה לשקף** ממונה השקפים, עם שם וסוג לכל אחד. קודם הדרך היחידה משקף 12 לשקף 1 הייתה לעבור את כולם.
- **גרירת קובץ תמונה לבמה** יוצרת אובייקט תמונה בנקודת השחרור. `addObject` רוכז לפונקציה אחת שמקבלת מיקום, ומשרתת גם את הכפתורים.
- **״צבעוני״ נבנה מהצבע שנבחר**, בתחביר צבע יחסי ב־oklch עם נפילה למדרג הערכה. במקביל נמצא עותק שני של הכפייה ל״צבע אחיד״ — בסרגל הצף, כלומר בדיוק במקום שבו משתמשים בזה — והוסר.

### אימות

- עשרים ואחת בדיקות עוברות; הבדיקה בדפדפן מכסה מתג העריכה, לחיצה כפולה בלי שום פאנל פתוח, רשימת הקפיצה, גרירת תמונה אמיתית לבמה, ומדרג שנגזר משלושה צבעים שונים.
- מעבר מלא על המצגת, מקשי המרצה, גרירה, שכבות וייצוא נייד — בלי שגיאות בקונסולה.

## סרגלים צפים באמת, ומחיקה — 2026-09-10

שלוש הערות אחרי הסבב הקודם, כולן נמדדו לפני התיקון.

- **סרגל הצד נעלם.** הכפתור היחיד שפתח אותו עבר לתוך הסרגל הצף, שקיים רק במצב עריכה. מי שיצא ממצב עריכה נשאר בלי דרך חזרה. נוסף כפתור **שקפים** קבוע בסרגל התחתון.
- **הסרגל הצף היה נעול.** הוא נמדד ב־500 על 97 פיקסלים בתוך במה של 1000 על 563 — כמעט חצי מהרוחב, ובגובה שלושה שורות, מקובע לתחתית ומסתיר את השקף. עכשיו הוא שורה אחת בגובה 54, כפתורי ההוספה הם אייקונים עם tooltip, ויש ידית גרירה. אותה ידית נוספה גם לסרגל האובייקט. המיקום נשמר במפתח נפרד ב־localStorage, כי זו העדפת תצוגה ולא תוכן.
- **לא הייתה מחיקה.** הכפתור היחיד ישב בכרטיס האובייקט בסרגל הצד, שהיה עכשיו רחוק. נוסף כפתור מחיקה בסרגל הצף של האובייקט, ומקש **Delete** מוחק את הנבחר.

### אימות

- נמדד בדפדפן: גרירת הסרגל למקום חדש ושחזורו אחרי רענון, מחיקה במקש ובכפתור, פתיחה וסגירה של סרגל הצד בשני המצבים, וכל מה שנבדק בסבב הקודם — לחיצה כפולה, רשימת הקפיצה, גרירת תמונה, מדרג צבע. בלי שגיאות בקונסולה.

## סגירת סבב העיצוב — 2026-09-11

הסבב נסגר. המצגת חיה, `main` מכיל את הכול, ו־`dist/index.html` מסונכרן עם המקור.

### מה שנמצא בסגירה ותוקן

**המניפסט של הלוגואים היה קיים רק בזיכרון של הסשן.** `TASKS.md` הבטיח ש־26 הסמלים ״מוכנים כשיחליט״, אבל הקובץ ישב בתיקיית העבודה הזמנית של הסשן — שנמחקת יחד איתו. סוכן שימשיך מכאן היה מגלה שאין כלום, והמרצה היה מתבקש להעלות את הקבצים שוב. המניפסט נכנס ל־`assets/tool-marks.json` (26 סמלים, WebP שקוף בגובה 180, 246KB), כלומר הוא עכשיו חלק מהפרויקט ולא מהסשן.

`TASKS.md` נכתב מחדש: הוא צמח בהוספות לאורך הסבב ולא ניתן היה לקרוא ממנו ״איפה אנחנו״. עכשיו יש בו מצב נוכחי, מה פתוח ולמה, ומה שחוזר על עצמו בעבודה — כולל שתי מלכודות שנתקלנו בהן יותר מפעם אחת: הענף שמתפצל אחרי כל squash, ואימות פרסום לפי גודל קובץ שאינו אמין כששינוי שוקל בדיוק כמו קודמו.

### מצב המערכת בסוף הסבב

- טבלאות: 12 פלטות, 5 פריסות, 5 גדלי כותרת, 3 סידורי רשימה, 5 מראות טקסט, 9 צורות, 12 רקעים, 5 רכיבים חזותיים. כולן נקראות מהטבלה — אין ענף ברנדרר.
- המצגת עצמה משתמשת בארבע פלטות בלבד, לפי משמעות.
- 31 בדיקות, הליכה מלאה נקייה, ופרסום אוטומטי מ־`main`.

## למה ״עיצוב״ נראה מקולקל, והזכוכית שלא מסגרה כלום — 2026-09-10

המרצה: ״צריך לעשות קצת סידור בסקציית ה'עיצוב' כי כל מה שקורה שם לא רלוונטי או לא משפיע (ואני לא מבין למה)״. הוא צדק, ויש לזה סיבה אחת פשוטה.

**נמדד:** לחיצה על כל שבע הערכות השאירה את הבמה על `rgb(13,22,40)` — בדיוק אותו צבע. הן שינו רק את הרקע *מסביב* לבמה. הסיבה: כל שקף נושא `palette` מפורש, ו־`paletteStyle()` כותב את כל הטוקנים inline על ה־`.slide` — כלומר דורס את הערכה לחלוטין. ברגע שכל 35 השקפים הוצמדו לפלטה, לערכה לא נשאר מה לשלוט בו.

- נוסף `deck: "לפי ערכת המצגת"` כאפשרות **הראשונה** בטבלת הפלטות, כלומר גם ברירת המחדל לשקף חדש. `paletteStyle()` מחזיר עבורה מחרוזת ריקה — וזה בדיוק מה שמאפשר לטוקנים להיפתר מול `[data-theme]` שעל המסמך במקום מול השקף.
- הפאנל אומר עכשיו למה הוא נראה מת: ״35 מתוך 35 השקפים נושאים ערכה משלהם, ולכן הערכה שלמעלה לא משנה אותם״, ומציע כפתור אחד שמחזיר את כולם.
- נמדד אחרי: עם שקפים משוחררים, carbon → wine → forest → paper נותנים ארבעה רקעי במה שונים. שקף בודד עדיין יכול להצמיד לעצמו ערכה.

### הזכוכית

המרצה: ״תקעת את הזכוכית פה בלי היגיון. היא אמורה להיות כמו מסגרת לטקסט לא סתם משהו תקוע בצד״. נכון — היא ישבה בפינה ריקה ולא מסגרה דבר. היא עברה לשקף שלושת הכלים, מחושבת מגבולות שלושת הוורדמארקים עצמם ועם ריפוד, וראשונה במערך האובייקטים כדי שתשב מאחוריהם. עכשיו היא מחזיקה תוכן במקום להתלוות אליו.

### אימות

- `npm test` — 31 בדיקות. הליכה מלאה קדימה ואחורה בלי שגיאות קונסולה.
- נמדד בדפדפן: שבע הערכות לפני ואחרי, שחרור כל השקפים, הצמדה חוזרת של שקף בודד ללבן, והמסגרת סביב הוורדמארקים.

## ערכות נושא לכל שקף, וצבע שיודע לוותר על עצמו — 2026-09-10

- **שני האפקטים רצו לכיוון הלא נכון.** הרכיבים שמהם הם נלקחו נכתבו לאנגלית. נמדד: מיקום הגרדיאנט עלה מ־32% ל־250%, כלומר שמאל→ימין. עכשיו הוא יורד מ־114% ל־‎-100%, והאור מגיע מהמקום שבו המשפט מתחיל.
- **ארבע ערכות חדשות** — ״לבן ודיו״, ״ערפל בהיר״, ״חול וחום״ ו״כחול חצות״. בערכות הבהירות `raised` ו־`soft` יורדים כהים יותר מהרקע ולא בהירים ממנו, אחרת כל שבב ופאנל נעלמים.
- **פאנל ״עיצוב״ מציע גם את ערכת השקף הנוכחי.** ערכה מלבישה מצגת, פלטה מלבישה שקף אחד — והמרצה מחפש את שתיהן באותו מקום.

### שני באגים שהערכות הבהירות חשפו

- **`.slide` צבע רקע מהפלטה ולא צבע טקסט.** צבע הטקסט הגיע בירושה מ־`body`, כלומר נפתר מול ערכת המצגת ולא מול פלטת השקף. זה עבר בשקט רק כי כל הפלטות היו כהות עם טקסט כמעט־לבן, ונשבר לגמרי ברגע שאחת לא הייתה. `.slide` פותר עכשיו `color: var(--text)` בעצמו.
- **אובייקט חופשי נושא צבע קבוע.** עשר תיבות טקסט היו `#f6f7f8` — בלתי נראות על נייר לבן. נוסף הערך `auto`: האובייקט לוקח את צבע הטקסט של השקף. הוא נבדק בכל מקום שבו צבע שמור הופך ל־CSS — צורות, קווים, ורכיבים חזותיים; ברכיבים הוא נכשל בשקט קודם, כי `color-mix()` עם מחרוזת לא חוקית פשוט לא מצייר. ספקטרום נבנה מ־hex אמיתי, ולכן `auto` נשאר צבע אחיד.

### אלמנטים

`dots` (סריג 6×4 קבוע, לא אקראי — טקסטורה מכוונת שנשארת זהה בין רינדורים) ו־`rule` (קו שיער) הצטרפו לטבלת הצורות. הוצבו קווים תחת שלוש כותרות ציר, שדות נקודות בשני שקפים, והזכוכית בשקף ״אותה מטרה. חלון אחר.״ — היכן שהיא גם אומרת משהו ולא רק מקשטת. כולם ב־`auto`, ולכן החלפת פלטה לוקחת אותם איתה.

### אימות

- `npm test` — 31 בדיקות. הליכה מלאה קדימה ואחורה בלי שגיאות קונסולה.
- נמדד בדפדפן: כיוון שני הסחפים, 12 ערכות בפאנל עם סימון הנוכחית, מעבר לשקף לבן ששורד רענון, וטקסט קריא בערכות בהירות בשלושה סוגי שקפים.

## לשבור את התבנית: פריסות, סקאלה, פלדה והדגשות — 2026-09-10

המרצה: ״עדיין יש משהו תבניתי מידי… תמיד תמיד תמיד יש כותרת כזאת גדולה באמצע… הקומפוזיציה נראית אותו דבר כל הדרך עד החלק האחרון של התמונות והסרטונים ופתאום זה מרגיש לי ממש הקלה.״ תיאור מדויק של רנדרר שידע לעשות דבר אחד: לרכז עמודה.

- **פריסה, סקאלה וסידור הם טבלאות.** `LAYOUTS` (מרכז, פינה, שוליים, נמוך, רחב), `SCALES` (לפי אורך / קטן / בינוני / גדול / ענק) ו־`ARRANGEMENTS` לרשימות (זו מתחת לזו / בשורה / מדרגות). השקף נושא `layout-*`, `scale-*` ו־`arrange-*`, וה־CSS מחליט מה זה אומר. אין ענף ברנדרר.
- **כל 35 השקפים קיבלו פריסה שנבחרה.** מרכז נשאר היכן שהוא הפואנטה; אותה צורה לא חוזרת שלוש פעמים ברצף מחוץ לרצף התמונות.
- **באג RTL בכל הפריסות:** ה־`scene` הוא flex column, ולכן `align-items` פועל על ציר ה־inline — וב־RTL ‏`flex-end` הוא הצד ה**שמאלי**. הכותרת נראתה נכון רק כי היא מילאה את הרוחב, בעוד הכיתוב הוצמד לשוליים הרחוקים. הכול עבר ל־`flex-start`, וה״מדרגות״ מזיזות ב־`margin-inline-start`.
- **הצבע:** אדום ובורדו ירדו מהמצגת. נוספה `steel` — בלי גוון בכלל, ולבן מלא — והיא נושאת את חמשת הרגעים שבהם משהו נשבר. הצבע מתנקז מהשקף במקום שהקיר יהפוך אדום. `ice`, `cobalt`, `steel` ושקף `amber` יחיד הם כל המצגת.
- **״ברק חולף״ נכנס לשימוש לראשונה** — כותרת הפתיחה ושקף המעבר — ו״זוהר נודד״ על שקף הפלדה היחיד.
- **הדגשה בתוך משפט:** `*כוכביות*` הופכות למילה מודגשת בצבע ההדגשה של השקף. הטקסט עובר escape **לפני** ההמרה, ולכן מסמך לא יכול להזריק HTML. המחרוזת השמורה שומרת את הכוכביות, ועריכה במקום קוראת אותה חזרה — המרצה עורך את מה שכתב, לא את מה שרונדר. **לא נוספה אף מילה לתוכן; רק סומנו מילים קיימות.**
- **סימן הכלי עבר מאובייקט חופשי לשדה של סוג השקף,** ומרונדר בתוך ה־eyebrow לצד שם הכלי. אובייקט חופשי מוצמד למרכז הבמה התרחק מהמילים ברגע שהפריסה לא הייתה מרכזית. זה גם חשף שישה סימנים ששלחתי בעצמי עם `opacity: "1"` — הסכימה שומרת שקיפות ב־0–100, כלומר הם היו אחוז אחד גלויים.

### עוד בסבב

- **״מודל שפה גדול״ רץ הפוך במסמך** — `["גדול.","שפה.","מודל."]`. הרנדרר היה תקין. הסדר תוקן יחד עם הכיתובים.
- **מצב הצגה השאיר מסגרת:** הבמה מחושבת סביב סרגל שמצב ההצגה מסתיר בלי לשחרר את המקום. עכשיו החלון כולו, והשארית במסך שאינו 16:9 שחורה וסימטרית. `‎.theater:fullscreen` יושב מאוחר יותר בגיליון באותו משקל וניצח בשקט — צריך היה לומר את זה פעמיים.
- **הלוגואים:** `tools/rasterize-logos.mjs` מצייר כל סמל מחדש בדפדפן מקומי לגובה אחיד ומוציא WebP שקוף — SVG לעולם לא נכנס למסמך. זה חשף שהמסמך נשא PNG בגודל מלא, וש**סמל ChatGPT הועתק שלם לשמונה שקפים**: אותו אומנות בגודל שבו הוא באמת מוצג חוסך 487KB, רבע מהמסמך.
- **שקף שלושת הכלים** נושא עכשיו את הוורדמארקים האמיתיים בשורה אחת, בגובה אות אחיד ובמרווחים שווים, מימין לשמאל — הסדר שבו הם נאמרים, שלא היה כך.

### אימות

- `npm test` — 31 בדיקות. הליכה מלאה על 35 השקפים קדימה ואחורה בלי שגיאת קונסולה.
- נמדד בדפדפן: יישור הכיתוב בכל פריסה, המדרגות (1261 → 1227 → 1192), מצב הצגה ב־1920×1080 (ללא פסים) וב־1512×982 (66px סימטרי), ההדגשה מרונדרת ושורדת עריכה במקום, ושתי ההנפשות רצות.
- **פתוח:** מסך צר מול הפריסות החדשות, וכל סוגי השקפים בתוך העורך.

## העותק בדפדפן הסתיר כל פרסום — 2026-09-10

המשתמש דיווח שהוא לא רואה את השינויים ושאל אם בוצע מיזוג. המיזוג בוצע, האתר הגיש את הבנייה החדשה בייט־בבייט — והמסך שלו עדיין הראה את הישנה.

הסיבה בשורה אחת ב־`app.js`: אם יש עותק ב־localStorage הוא מנצח תמיד את המסמך המוטמע. זה נכון — זו העבודה של המרצה — אבל המשמעות הייתה שכל פרסום מכאן והלאה בלתי נראה למי שפתח את ההרצאה אי פעם, בלי שום סימן שמה שמולו ישן.

- המסמך נושא עכשיו `revision`, שנחתם בזמן הבנייה מה־hash של התוכן עצמו — משתנה בדיוק כשההרצאה משתנה, ולא תלוי בכך שמישהו יזכור לעדכן מספר.
- בטעינה, כשה־revision של המסמך המפורסם שונה מזה של העותק השמור, **העותק השמור עדיין נטען** ומופיעה שורה על הבמה: ״טעינת הגרסה החדשה״ או ״להישאר עם שלי״.
- ״טעינה״ שומרת קודם את העותק הנוכחי כטיוטה ואז מחליפה. ״להישאר״ רושם את ה־revision החדש על העותק השמור, כך שהשורה לא חוזרת לשאול.

### אימות

- נמדד בדפדפן על התרחיש המלא: עותק ישן שמור → השורה מופיעה והעריכה של המשתמש עדיין על המסך; ״להישאר״ → העריכה שורדת רענון והשורה נעלמת; ״טעינה״ → המסמך המפורסם נטען והעותק הקודם מופיע ברשימת הטיוטות. בלי שגיאות קונסולה.
- `npm test` — 31 בדיקות עוברות.

## ארט־דירקשן: מערכת צבע אחת, רקע חיבורים, ותיקון המעבר — 2026-09-10

המשתמש דיווח שהמצגת מרגישה מונוטונית — ״רצף של כותרות״ — ושהצבעים לא הגיוניים: הרבה פלטות לאורך הדרך במקום כמה שמתכתבות. מדידה על המסמך אישרה את זה במספרים: 7 פלטות, 12 רקעים, 5 תנועות ו־4 מעברים פזורים על 35 שקפים כמעט בלי חזרה על צירוף — גיוון מפוזר שווה במקום מערכת.

- **ארבע פלטות עם תפקיד.** `ice` היא הבית (פחם ותכלת), `cobalt` היא הקרקע העמוקה יותר מרגע שההרצאה עוסקת בעשייה, `danger` מופיעה אך ורק היכן שמשהו נשבר (חמישה שקפים), ו־`amber` מופיעה פעם אחת בדיוק — ברגע האנושי היחיד. `violet`, `rose` ו־`mint` יצאו מהמצגת ונשארו זמינות בעורך.
- **רקע, תנועה ומעבר לפי משמעות.** 27 שקפים על `plain`; `grid` נשאר רק היכן שהמכונה עצמה היא הנושא; `halo` בשתי נשימות; `cut` נוחת עם כל שקף אדום; `push` רק בתוך שלישיית התמונות; `zoom` פעם אחת, בכניסה לשקף הסיכום. `spectrum` ירד מ־18 שקפים לארבעה — ארבעת המשפטים שההרצאה תלויה בהם.
- **רקע ״רשת חיבורים״.** המשתמש ביקש במפורש את מראה רשת הנוירונים מסרטון של גוגל. נוסף `neural` כשורה בטבלת הרקעים: נקודות וקווים דקים שנבנים מזרע השקף (ולכן זהים אחרי כל רינדור), מרכז הפריים נשאר ריק, והכול נגזר מ־`--accent-rgb` בלבד. הקווים נמשכים באמצעות `stroke-dasharray` באורך הקו עצמו, כך שאותה אנימציה גם מציירת אותם. ארבעה שקפים משתמשים בו.
- **סימן הכלי כסימן מחלקה.** שקפי הדמו נראו כמו שקפי משפט. הסמל של הכלי יושב עכשיו במקום קבוע אחד מעל ה־eyebrow בכל שקף דמו עם קישור אמיתי — ״עוד רגע יוצאים לכלי עצמו״ — ושקפי המותג נשארו נפרדים ונדירים.

### הבאג במעבר בין שקפים

השקף ״קפץ שמאלה וחזר״ לפני שהמעבר קרה. `.entering` לא הוסר אף פעם, ולכלל הכניסה ולכלל היציאה יש אותו משקל — ולכן השקף היוצא הריץ מחדש את אנימציית *הכניסה* שלו. בנוסף `--dir` על השקף היוצא נשאר מהתנועה שהביאה אותו, ולא מזו שמוציאה אותו.

נמדד בדפדפן לפני ואחרי. לפני: היוצא נע מ־x=127 אל 0 בזמן שהנכנס נע מ־‎-127 אל 0 — שניהם מתכנסים פנימה מהצדדים. אחרי: היוצא נע מ־0 אל 127 והנכנס מ־‎-127 אל 0 — רצועת סרט אחת, וזהה בין אם הגעת קדימה או אחרי צעד לאחור.

### אימות

- `npm test` — 31 בדיקות עוברות. הליכה מלאה על 35 השקפים קדימה ואחורה בלי שגיאת קונסולה, ובלי שקף שנושא את שתי המחלקות בו־זמנית.
- צילומי מסך של שקפי המפתח לפני ואחרי.
- **פתוח:** תיקיית הלוגואים של המשתמש נמצאת אצלו במחשב ולא הגיעה לסשן. במסמך יש כרגע ארבעה סמלים בלבד (ChatGPT, Claude, Gemini, NotebookLM).

## המשך לאחר משוב על אב־הטיפוס

### תיקון אינטראקציית טקסט ותזמון אובייקטים — 2026-09-10

- בחירת אובייקט אינה מרנדרת עוד את הבמה מחדש בזמן הלחיצה הראשונה, כדי שרצף הדאבל־קליק המקורי של הדפדפן יגיע ליעדו. לחיצה כפולה פועלת גם על טקסט מובנה וממירה אותו לתיבה מקושרת.
- עריכת טקסט מסתיימת בלחיצה מחוץ לתיבה, באיבוד מיקוד, ב־Esc או ב־Ctrl+Enter. הלחיצה מחוץ לבמה אינה משאירה `contenteditable` תקוע.
- המראה הצבעוני הועבר לבורר מפורש של שלושת מצבי הטקסט בסרגל הצף; הוא משתמש באותו גרדיאנט בהיר של הכותרות המקוריות.
- בחירת כניסה או יציאה מציגה תצוגה מקדימה מבודדת דרך Web Animations בלי להפעיל מחדש את כל השקף.
- זמן השארת השקף היוצא נגזר כעת ממשך המעבר וממשך יציאת האובייקטים במקום timeout קבוע של 900ms. כניסת השקף הבא מתחילה רק לאחר סיום היציאה, והשקף האטום הקודם מוסר מיד לאחר מכן.

### ביטול, סרגל אובייקט מלא ותיקון הלחיצה הכפולה — 2026-09-10

המשתמש דיווח שהעריכה ״באגית״: אין ביטול, לחיצה כפולה על תיבת טקסט אינה פותחת אותה, תיבה חדשה נתקעת, אין שליטה נוחה על גודל טקסט, אי אפשר להחליף סוג צורה מהסרגל שעל הבמה, ו״שדה כוכבים״ מופיע כרכיב אף שהוא רקע. כל אחד מהם נמדד לפני התיקון.

- **השורש של הלחיצה הכפולה.** `startObjectPointer` קרא ל־`preventDefault()` על `pointerdown` ותפס את הסמן על הבמה. הראשון מבטל את אירועי העכבר התואמים, השני מפנה את ה־click לאלמנט שתפס — ולכן הדפדפן מעולם לא דיווח על `dblclick` מעל אובייקט חופשי. נמדד: שני `pointerdown` בלי `click` ובלי `dblclick`. עכשיו ההגנה מפני בחירת טקסט וגרירת תמונה נעשית ב־CSS, והתפיסה מתחילה רק אחרי תזוזה של 3 פיקסלים — מה שגם מונע הזזה של אובייקט בלחיצה יציבה.
- **סיום עריכה חדל לבנות את הבמה מחדש.** התיבה מנוקה במקומה, והרינדור המלא נדחה ב־420ms ומתבטל בכל לחיצה חדשה, כדי שלא יחליף את הצומת בין שתי הלחיצות. יציאה מתיבה אחת כבר לא ״אוכלת״ את הלחיצה שבוחרת את הבאה.
- **ביטול וביצוע חוזר.** מחסנית טהורה ב־`core` (`createHistory`) שומרת מסמכים מאומתים כמחרוזות. `save` הוא גם נקודת הרישום, ופעולה רציפה — משפט שהוקלד, סליידר צבע שנגרר — מקבלת token ונרשמת כצעד אחד; ה־token נושא מונה סשן, כך שחזרה לאותה תיבה מאוחר יותר היא צעד נפרד. שני כפתורים בסרגל התחתון, `Ctrl+Z`/`Ctrl+Y`, וקפיצה לשקף שבו השינוי קרה. בתוך שדה כתיבה הביטול נשאר של הדפדפן.
- **סרגל האובייקט הפך למלא.** גודל גופן בכפתורי צעד עם מספר מדויק, משקל, יישור, מראה וצבע לטקסט; סוג הצורה, המראה, המילוי והקו לצורה; סוג הרכיב, מראה ושני צבעים לרכיב; התאמה והחלפת קובץ לתמונה — ולכולם שכבות, שכפול ומחיקה. שינוי מראה של טקסט נכתב ישירות על האלמנט, כך שהעריכה שורדת אותו.
- **מראה צבעוני לצורות ולרכיבים.** `FILL_STYLES` ו־`VISUAL_STYLES` הן טבלאות כמו כל השאר, והשדה `style` מקבל ברירת מחדל כשהוא חסר — כך מסמכים קיימים ממשיכים לעבור אימות. הצורה נצבעת בגרדיאנט SVG שנגזר מהצבע שנבחר, והרכיב בגרדיאנט CSS מאותו חישוב.
- **שדה כוכבים עבר לרקעים.** הוא הוסר מ־`VISUALS` ונוסף ל־`BACKDROPS`; מסמך ישן שמכיל אובייקט כזה ממיר אותו לרקע השקף במקום להיכשל באימות.
- **תיקונים שנמצאו בדרך.** ידית שינוי הגודל לא הופיעה עד הרינדור הבא כי היא נבנתה רק לאובייקט הנבחר — עכשיו היא חלק מכל אובייקט ומוצגת ב־CSS. סרגל צף שמתחיל ב־`left:50%` יכול לנצל רק חצי מרוחב הבמה, ולכן נפרש לארבע שורות של 181px (38% מגובה הבמה); מרכוז בשני הקצוות החזיר אותו לשורה או שתיים, ובמסך צר הוא שורה אחת נגללת. סרגל שמכסה את האובייקט הנבחר עובר לתחתית. טקסט צבעוני נעלם בזמן עריכה כי פאנל העריכה דרס את הגרדיאנט; עכשיו הוא מוצג בצבעו המלא כל עוד הסמן בתוכו. בחירת צבע כבר לא מבטלת את המראה הצבעוני שנבנה ממנו. סרגל הצד אינו נבנה מחדש כשהוא סגור. לחיצה על במה ריקה משחררת את הבחירה.

### אימות

- עשרים וחמש בדיקות אוטומטיות עוברות, ובהן ארבע חדשות: המרת שדה הכוכבים לרקע, ברירת המחדל ואיסור ה״קו מתאר״ ברכיב, מסלול ביטול/ביצוע חוזר, וכיווץ רצף הקלדה לצעד אחד עם תקרה למחסנית.
- נבדק בדפדפן (Chromium, 1400×900 ו־400×780, גם עם `prefers-reduced-motion`): לחיצה כפולה שפותחת תיבה, הקלדה, Esc, לחיצה כפולה חוזרת; ביטול והחזרה של הקלדה, גרירה ושינוי גודל, בכפתורים ובמקלדת; גרירה שזזה ולחיצה שאינה מזיזה; סרגל לכל ארבעת סוגי האובייקטים; גרירת בורר צבע חי בלי לאבד את הגרירה; כל עשרת סוגי השקפים קדימה ואחורה; ייצוא נייד; ובמסך צר בלי גלילה אופקית. הקונסולה נקייה בכל המסלולים.
- לא נבדק: מסך מגע אמיתי ו־Safari. הבדיקות רצו ב־Chromium בלבד.

### שכבות, חיתוך, טיוטות ותיקון הטקסט שמתחת לשכבה — 2026-09-10

- **טקסט מובנה חזר להיות לחיץ.** `‏.editing .free-object-layer` קיבל `pointer-events: auto`, והשכבה פרושה על כל הבמה — כך שאובייקט חופשי אחד בשקף הספיק כדי שכל כותרת שמתחתיו תפסיק להגיב ללחיצה כפולה. נמדד: `elementFromPoint` על הכותרת החזיר את השכבה. עכשיו השכבה שקופה לסמן והאובייקטים עצמם לוכדים אותו.
- **פאנל שכבות.** רשימת האובייקטים של השקף מהקדמי לאחורי, עם בחירה, העברה קדימה ואחורה ומחיקה. נפתח מכפתור בסרגל השקף ונגרר כמו שאר הפאנלים.
- **חיתוך תמונה.** לאובייקט תמונה נוספו `zoom`, `focusX` ו־`focusY`, כולם עם ברירת מחדל כשהם חסרים כדי שמסמכים קיימים ימשיכו לעבור אימות. במצב חיתוך הגרירה מזיזה את התמונה בתוך המסגרת ולא את המסגרת על הבמה, והסטפר מגדיל עד פי ארבעה. העדכון נכתב ישירות על האלמנט כדי שהגרירה תישאר חלקה.
- **טיוטות שמורות.** שמירה בשם, פתיחה (שנרשמת בהיסטוריה ולכן ניתנת לביטול), עדכון, מחיקה והורדה של כל טיוטה כ־JSON או כקובץ HTML עצמאי. עד 25, וכשל מקום מקבל הודעה שמסבירה מה לעשות. **PPTX נשקל ונדחה**: הוא דורש ספרייה חיצונית, והמיפוי היה מאבד תנועה, רקעים ושכבות.
- **הוספה ומחיקה של שקפים על הבמה.** המשתמש לא מצא אותן — הן היו רק בסרגל הצד, ומחיקה דרשה לפתוח את הסעיף של השקף. עכשיו יש בורר סוג, הוספה אחרי הנוכחי, שכפול ומחיקה בסרגל השקף עצמו.
- **שם קובץ בהורדה.** נמדד: כרום מוותר על ערך לא־ASCII ב־`download` בעמוד `file://` ושומר את הקובץ בשם `download` בלי סיומת — כלומר הייצוא הקיים ירד בלי שם ובלי סיומת. השמות עכשיו באנגלית, וההודעה אומרת מה ירד.

### אימות

- עשרים ושש בדיקות עוברות, ובהן חדשה לחיתוך: ברירות מחדל למסמך ישן, שמירת ערכים תקינים ודחיית ערכים מחוץ לתחום.
- נבדק בדפדפן: לחיצה כפולה על כותרת בשקף שיש בו אובייקט; פתיחת פאנל השכבות, סדר קדמי־אחורי, בחירה מהרשימה והעברה שמשנה את סדר הציור בפועל; חיתוך — סטפר, גרירה שמזיזה את התמונה ולא את המסגרת, ואיפוס; מחזור טיוטות מלא כולל הורדות ושרידות רענון; הוספה, שכפול ומחיקה של שקף והחזרתו בביטול; מסך צר; והרגרסיה המלאה עם `prefers-reduced-motion`. הקונסולה נקייה.

### מצב הצגה, סרטונים ורכיבים נוספים — 2026-09-10

לפני הסבב הזה נשאלה שאלת ארכיטקטורה: להישאר ב־HTML או לעבור לפריימוורק / לעצב ב־Figma. **ההחלטה: להישאר.** היתרון המכריע הוא שהתוצר הוא קובץ אחד שנפתח בכל מחשב בלי התקנה, בלי רשת ובלי build — וזו דרישת המוצר עצמה, לא נוחות. Figma הייתה לוקחת את הצעדים, הטיימר, הערות המרצה והניסוי האינטראקטיבי, ומחזירה סבב ייצוא־ייבוא על כל תיקון טקסט. החיסרון שנרשם בגלוי: `app.js` הוא כמעט 3,000 שורות עם רינדור ידני, ורוב הבאגים האחרונים נבעו מכך. הגבול שסוכם: לא להרחיב עוד את כלי העיצוב, ולהתקדם לתוכן.

- **מצב הצגה.** מצב מפורש (`P`, כפתור, יציאה ב־Esc) שמסתיר סרגל, נקודות, רמז מקלדת וכל כלי העריכה, נכנס למסך מלא ומעלים את הסמן בחוסר תנועה. הכפתורים שיושבים על השקף — טיימר, פרומפט, מצב הניסוי, הפעלת סרטון — נשארים, כי איתם מנחים את ההרצאה.
- **שקף סרטון.** יוטיוב וגוגל דרייב בלבד. מהכתובת שהודבקה נלקח **רק המזהה**, מול טבלת מקורות; כתובת גולמית לעולם אינה מגיעה ל־iframe. הכתובת להטמעה נבנית ב־`core`.
- **הסרטון לא נטען עד שלוחצים.** עד אז מוצגת תמונת פוסטר ששמורה במסמך. כך פתיחת המצגת אינה פונה לרשת כלל, והשקף נראה שלם גם בלי אינטרנט. זו הסיבה שכלל ה״אין נכסי רשת״ ב־AGENTS נוסח מחדש כחריג מתוחם ולא בוטל.
- **שלושה רכיבים חדשים** — כדור זוהר, עמודות חיות וחלון כלי — ורקע **מרחב צבע**. במקביל הומרו הרקעים והרכיבים לטבלאות בונים לפי מפתח, במקום שרשרת `else if` במסלול הרינדור, כפי שהסכם העבודה דורש.

- **סרטון מקומי הוא הדרך המרכזית.** לבקשת המשתמש, המקור המועדף הוא קובץ שיושב בתיקייה `videos` ליד קובץ המצגת. הנתיב מאומת כנתיב יחסי בלבד — בלי סכימה, בלי אות כונן, בלי `/` פותח ובלי `..` — כדי שמסמך שהגיע מבחוץ לא יכוון את הדפדפן לקובץ שרירותי בדיסק. הקבצים עצמם אינם נכנסים למסמך ואינם נכנסים לגיט (`dist/videos/` ב־`.gitignore`).
- **בחירת קובץ מהעורך מנגנת אותו מיד** מתוך הזיכרון של אותו סשן, בזמן שהמסמך שומר את הנתיב. קובץ חסר אומר זאת על השקף במקום להישאר שחור.

### אימות

- עשרים ותשע בדיקות עוברות, ובהן שתיים חדשות: פענוח קישורי וידאו (כולל דחיית `javascript:`, ספק לא נתמך ומזהה עם תווים זרים) ואימות שקף וידאו עם קישור ריק ועם קישור פסול.
- נבדק בדפדפן: מצב הצגה מסתיר את הכרום ומקשי הניווט ממשיכים לעבוד, Esc יוצא; הוספת שקף וידאו, קישור פסול שנדחה עם הסבר חי, קישור תקין שמזוהה, פוסטר עם כפתור הפעלה ובלי iframe, לחיצה שיוצרת בדיוק את כתובת ההטמעה שנבנתה, ומעבר שקף שעוצר את הסרטון; שלושת הרכיבים החדשים בשני המראות; והרגרסיה המלאה.
- **וידאו מקומי נבדק מקצה לקצה**: הוקלט קובץ webm אמיתי, הונח ב־`dist/videos`, והמצגת נפתחה מ־`file://` — הנגן הגיע ל־`readyState 4` עם משך ורוחב אמיתיים ובלי שגיאה. כלומר וידאו מהמחשב עובד בפתיחה ישירה, בלי שרת ובלי רשת. קובץ הבדיקה נמחק ולא נכנס לגיט.
- **לא נבדק, ודורש בדיקה שלך:** האם הטמעת יוטיוב אכן מתנגנת מ־`file://`. הרשת חסומה בסביבת הפיתוח, אז נבדק רק שהמבנה והכתובת נכונים. זה נוגע רק למסלול המרוחק, שהוא כבר לא המסלול המרכזי.

### שפת העיצוב נכתבה כדרישה — 2026-09-10

המשתמש ביקש שהסגנון לא יישאר ידע שבעל פה: ״מינימליסטי, עתידני יפה והייטק, עם מעברים יפים, הדגשה בהתאם לתוכן, שימוש באלמנטים בהתאם לתוכן ההרצאה ולאפשרויות הפלטפורמה, ושימוש בתמונות, לוגואים של כלים וסרטונים מהמחשב שלי״.

- נוסף פרק **״The look this deck is held to״** בראש `AGENTS.md`, מיד אחרי תיאור המוצר ולפני הארכיטקטורה — כלומר כל סוכן קורא אותו לפני שהוא נוגע בקוד או בתוכן. הוא מנוסח כדרישה ולא כהמלצה, ומתורגם לפעולות: רעיון אחד ענק בשקף, מעבר ותנועה שנבחרים לפי מה שהם אומרים, הדגשה שנגזרת מהמשפט, בחירת רכיב לפי המשמעות שלו (כדור למודל, עמודות להסתברויות, חלון לכלי), חומר אמיתי לפני איור שלו, וריסון בצבע.
- `CLAUDE.md` מפנה אליו במפורש ומוסיף שסוכן אמור לומר כשבקשה מושכת את המצגת מהקו הזה.
- `README.md` קיבל את אותו קו בעברית, למרצה, כדי שגם הוא ידע לפי מה לבחור.
- **קישורים לכלים.** לשקפי ״הדגמה חיה״ ו״תמונה״ נוסף שדה קישור שמייצר כפתור ״פתיחת <הכלי>״, ולאובייקטים חופשיים (טקסט ותמונה) נוסף אותו שדה — כך שלוגו של כלי הוא כפתור. מתקבל `https://` בלבד; `javascript:`, `data:`, `file:` ו־`http:` נדחים באימות ובעורך, עם הסבר חי. הקישור נפתח בלשונית חדשה עם `noopener noreferrer`, ובאובייקט הוא מושבת במצב עריכה כדי שהלחיצה תמשיך לשייך לעורך.
- הובהר גם **איפה בדיוק לשמור סרטונים** — תיקיית `videos` באותה תיקייה שבה יושב קובץ ה־HTML שנפתח, עם דוגמה בנתיב ווינדוס ואזהרה על תיקיית ההורדות.

### דילוג על שקף — 2026-09-10

- לשקף נוסף `visibility` כטבלת בחירה (`VISIBILITY`), כך שמסמכים קיימים מקבלים ״מוצג״ כברירת מחדל ועוברים אימות בלי שינוי.
- הניווט ב־`core` מדלג בשני הכיוונים, `initialState` פותח על השקף המוצג הראשון, והספירה (`shownCount`, `shownPosition`) היא לפי מה שהקהל רואה.
- בממשק: כפתור עין בסרגל השקף ובסרגל הצד, תגית ״שקף מדולג״ על הבמה במצב עריכה בלבד, נקודה שנעלמת מהניווט, מונה שמראה `--` על שקף מדולג, ושורה מסומנת ברשימת הקפיצה כדי שהשקף יישאר נגיש לעריכה.

### אימות

- שלושים ואחת בדיקות עוברות, ובהן חדשה: שקף מדולג נשאר במסמך, הניווט עובר מעליו בשני הכיוונים, פתיחה על שקף מדולג נופלת על הראשון המוצג, וערך לא חוקי נדחה.
- נבדק בדפדפן: דילוג והחזרה, היעלמות הנקודה, המונה, המעבר בפועל קדימה ואחורה, הגעה לשקף מדולג דרך רשימת הקפיצה, וביטול שמחזיר אותו.

### סבב תיקונים על הגרסה החיה — 2026-09-11

תשע הערות מהמרצה אחרי מעבר על המצגת. שמונה טופלו; אחת ממתינה לו.

- **התמונות.** המקורות הם 2560x1440, והם הוטמעו ב־1440x810 וב־11–44KB. זו הייתה הסיבה היחידה שהן נראו רע — לא התקציב: אותן יצירות ב־2200 רוחב ובאיכות 0.90 עולות 32–54KB כל אחת, כי הן שטחים לבנים גדולים עם מעברים חלקים. נוסף `tools/embed-artwork.mjs` שמקודד לתוך `content.json` לפי הנתיב שמחזיק את השדה, לא מגדיל רסטר מעבר למקור, ומרנדר SVG בכל גודל. חמש יצירות הוטמעו מחדש, מהן שתיים חדשות.
- **הצלבה שהראתה שני שקפים.** `t-fade-in` הורץ על כל תיבת השקף, כך שגם הרקע האטום שלו התמוסס ולשליש שנייה נקראה הכותרת היוצאת דרך הנכנסת. זה מה שהמרצה תיאר כשקף מחורבש. עכשיו התיבה נכנסת אטומה ומכסה מיד, ורק `.atmosphere`, `.scene` ו־`.free-object-layer` מתמוססים.
- **קומפוזיציות מוזרות.** `edge` ו־`low` שומרים שליש מהפריים פנוי למשהו שיישב לצד המילים. על `split` אין מה לפנות, ולכן שתי העמודות נדחסו לימין הפריים עם שדה מת לצידן. `SPANS_STAGE` מצהיר את הסוגים הרחבים פעם אחת, והפריסות מסויגות `:not(.spans-stage)` — כך שזה לא יחזור עם הסוג הרחב הבא.
- **רקעים.** 31 מתוך 36 שקפים היו `plain` ו־17 היו `still`, ולכן השקפים הלבנים היו לבן סטטי. עכשיו: `halo` מתחת ללוגואים, `arcs` (הקשתות שנכנסות, מהגרסה שהמרצה אהב), `mesh`, `grid`, `neural` ו־`beams` — plain ירד ל־13, ואף רקע אינו על יותר משישה שקפים.
- **מעברים.** `fade` היה על 18 מתוך 36. האיזון שנעשה בסבב הטונלי נדרס בקומיט הפלואו החדש. עכשיו: `cut` 12, `push` 12, `fade` 8, `zoom` 4, ואף אחד לא מעל שליש. גם התנועות אוזנו: `rise` ירד מ־21 ל־12.
- **ריצת הלוגואים.** שלושת הוורדמארקים נכנסים ויוצאים ב־`curtain` — אותה מחווה שהמרצה אהב במעבר Gemini→NotebookLM — במקום שלוש הצלבות זהות.
- **לוגואים מלאים.** Gemini והלוגו המלא של Gemini Notebook החליפו את שני הסמלים המרובעים. תיבת האובייקט נקבעת לפי יחס הלוגו עצמו (`width% / height% = aspect * 9 / 16`), כי הווילון גוזר את התיבה — תיבה רחבה מהלוגו הייתה נפתחת על שטח ריק לפני שהלוגו מופיע.
- **הנפשת הסוכן.** נוסף `autoplay` לשקף וידאו: `manual` או `once` — מתנגן פעם אחת, מושתק, בכניסה לשקף, ועם `prefers-reduced-motion` חוזר לפוסטר ולכפתור. נוסף שקף `lecture-agent-motion` **מדולג**, עם הפוסטר באיכות מלאה, שמחכה לקובץ `videos/agent-working.mp4`.

### שם ההרצאה וצילומי הכתבות — 2026-09-11

- **שם ההרצאה.** ״המדריך ל-2030״ ככותרת גדולה, ומתחתיה ״איך AI משנה את הדרך שבה אנו עובדים״ — עם מקף, כפי שהמרצה כתב אותו, ולא הנקודתיים שהיו. שקף הפתיחה היה שאלה לקהל עם שם ההרצאה כהערת שוליים ב־21px; עכשיו השם הוא השקף, והשאלה קיבלה שקף משלה מיד אחריו, כך ששום דבר לא נגרע. הטבלה ב־`LECTURE-FLOW.md` נבנית מעכשיו מתוך המסמך עצמו, כדי שהמספור לא ייצא שוב מסנכרון.
- **ארבעת צילומי הכתבות** הוטמעו מחדש מהמקורות ב־2400px במקום 1800px — **ובפחות בייטים**, כי WebP מנצח JPEG על צילומי מסך של טקסט.
- **שלוש תמונות חידון התמונות הן 728px גם במקור** שנשלח, ולכן הן נשארות כפי שהן. אי אפשר לחדד תמונה שאין לה מקור גדול יותר.

### אימות

- שלושים ותשע בדיקות עוברות, ובהן חדשה ל־`autoplay` (ברירת מחדל למסמך ישן, ערך תקין, וערך פסול שנדחה).
- `tools/stage-audit.mjs` — נקי על כל 36 השקפים. 46 הצעדים נבדקו קדימה ואחורה, 390px בלי גלישה, בלי שגיאות קונסולה.
- ההפעלה האוטומטית נבדקה בפועל מול קובץ WebM שנוצר לבדיקה: הנגן הגיע ל־1.17 שניות, מושתק, בלי לולאה, ונעצר בסוף; ובמצב תנועה מופחתת הוצג הפוסטר במקום. **לא נבדק** ה־mp4 של המרצה עצמו: לכרומיום שבסביבה הזאת אין מפענח H.264, בדיוק כמו ארבעת הסרטונים הקיימים.
- **לא תוקן, וחסרים מקורות:** תמונות חידון התמונות (728px), הפוסטרים של ארבעת הסרטונים (960x540) וצילומי הכתבות. הן לא היו בחבילה שנשלחה, ואי אפשר לחדד תמונה שאין לה מקור.

## הצעדים הבאים

1. לעבור על שקף הברכה ושקף הסיום מול המסך והאולם שבהם תוצג ההרצאה.
2. למלא לפני כל הרצאה את דוגמאות הקהל: שאלת חיפוש, מסמך מוכר, עובדה לבדיקה ופעולת סוכן.
3. להכין את מחברת NotebookLM ותיקיית הדמה של הסוכן, ולוודא את נקודות העצירה בארבעת הסרטונים.
4. לבנות חבילות דוגמאות קבועות לקהלים חוזרים רק אחרי שנמדד מה עובד על הבמה.

אין לבנות מראש עורך פריסה חופשי, מערכת חשבונות, תחזיות עתיד לא מבוססות או חיבור API מתוך הדפדפן עם מפתח סודי.

## ההרצאה המלאה — 2026-09-10

- מסמך התוכן הורחב לשלושת חלקי ההרצאה: 35 שקפים מוצגים ו־11 שקפי הניסיון המקוריים שנשמרו כמדולגים.
- סדר הבמה, גרסת 60 הדקות ובחירת הנכסים מתועדים ב־`LECTURE-FLOW.md`.
- הוטמעו צילום המרצה, סמלי ChatGPT, Claude ו־Gemini, לוגו NotebookLM מלא, ארבעה צילומי כתבות ושלושה זוגות תמונות לחידה. התמונות הותאמו לגודל המסמך, והמסמך המאומת נשאר מתחת לתקרת 2.4MB.
- צילומי הכתבות מקושרים למקורות; שקפי הדמו ולוגואי הכלים פותחים את Google, ChatGPT, Claude, Gemini, NotebookLM ו־Tiktokenizer. אין שימוש בלוגואים שלא מופיעים בפועל במהלך הנבחר.
- ארבעת סרטוני הבמה משתמשים בנתיבים מקומיים קבועים: `videos/musk.mp4`, `videos/figure-03-home.mp4`, `videos/figure-03.mp4` ו־`videos/seoul-world.mp4`. העותקים הונחו מקומית תחת `dist/videos/` ונשארים מחוץ ל־Git לפי המדיניות הקיימת.
- כפתור הפתיחה הוצמד לשקף המוצג הראשון במקום לאינדקס אפס, כדי שימשיך להופיע כששקפי ניסיון קודמים מדולגים.
- בדיקות שמוסיפות שקפים משתמשות במסמך בדיקה קומפקטי, כדי להישאר בלתי תלויות בגודל ההרצאה האמיתית. כל 31 הבדיקות עוברות לאחר הבנייה.
- לא בוצע סבב דפדפן חזותי במסגרת השינוי הזה. הבנייה, אימות הסכמה והייצוא העצמאי נבדקו אוטומטית; נקודות העצירה בסרטונים דורשות מעבר של המרצה לפני האירוע.

### סבב ארט־דיירקשן — 2026-09-10

- בעקבות משוב שהגרסה הראשונה מונוטונית, נוסף לכל שקף שדה `palette` מאומת עם שבע פלטות. הפלטה מחליפה את כל שדה האור של השקף — רקע, טקסט, הדגשה, קווים ומדרג — בלי לאפשר CSS חופשי במסמך מיובא.
- מגבלת השקפים עלתה מ־40 ל־48 כדי לשמור את 11 שקפי הניסיון המדולגים ובמקביל לצרף את כל חומרי ההשוואה והווידאו שביקש המשתמש.
- תוקנה שגיאת סימון ב־`frame`: מחלקות `motion-*` ו־`slide-text-*` נכתבו מחוץ למאפיין `class`, ולכן רוב הנפשות הכניסה ומראות הטקסט כלל לא הופעלו. הן כעת מחלקות אמיתיות.
- המסלול משתמש במעברים ובתנועות כניסה שונות לפי הקשר, אך רוב החיבורים בין רעיונות משתמשים במעבר שקט. המעברים החדים והזום נשמרים רק לפנייה או לשיא בטיעון.
- כל שלושת זוגות AI מול אמיתי משולבים במסך כמעט מלא, בשלושה סבבי בחירה ושקף תשובות מדורג. ריווח שקף `contain` ירד מ־5% ל־1.5%, וכותרת על תמונה מוצגת כתג זכוכית צף במקום לכווץ את התמונה.
- סרטון Figure 03 הביתי נוסף לפני סרטון הייצור הקיים, בשם המקומי `videos/figure-03-home.mp4`.

### תיקון ארט־דיירקשן בעקבות ביקורת — 2026-09-10

- כל רכיבי ה־orb, bars, glass, window ו־accordion שנוספו כהשלמת חלל הוסרו מהמסלול הפעיל. הרכיבים עצמם נשארו ביכולות העורך, אבל אינם מופיעים בלי תפקיד נרטיבי.
- אובייקטים חופשיים נשארו רק בארבעה מקומות עם פעולה ברורה: סמל ChatGPT במעבר Google→ChatGPT, קישור ממורכז ל־Tiktokenizer, שקף בחירת כלי, ושקף NotebookLM.
- נוסף שקף בחירה ייעודי עם הסמלים המקוצרים של ChatGPT, Claude ו־Gemini; כל סמל ושמו מקושרים לכלי שלו. סמל ChatGPT נחתך מהלוגו הארוך שסופק, בלי לייצר מותג חלופי.
- רגע NotebookLM פוצל לשניים: קודם הקונספט והפרומפט, ואחריו הלוגו המלא לבדו כקישור. כל הלוגואים הארוכים הוסרו מפינות שקפי הדמו.

### תמונה תחילה — 2026-09-10

- תג הכותרת של שקפי `contain` כבר אינו פאנל מרכזי. הוא מוצמד לימין־למטה, רוחבו מוגבל ל־42% מהבמה וגודל הכותרת מוגבל בנפרד, כך שהתמונה נשארת החומר המרכזי.
- כפתור המקור נשאר בפינה הנגדית כדי שלא יתחרה בתג. במסך צר התמונה אינה נדחסת כדי לפנות אזור טקסט; התג נשאר מעליה, ברוחב מוגבל ובטיפוגרפיה קטנה יותר.
- שפת תג הזכוכית משמשת כעת גם את שקפי Nobel Prize, Straits Times ו־Reuters, עם כותרת קצרה ושורת הקשר אחת. שקפי שלוש ההשוואות ושקף איטנה משתמשים באותו חוק פריסה.

### פתיחה, ברכה וסיום אישיים — 2026-09-10

- צילום הראש כבר אינו תמונת הפתיחה. הפתיחה משתמשת בתמונת מפתח מופשטת שנוצרה במיוחד למצגת: זרימה כחולה שמתכנסת לנקודת מעבר והופכת למסלול פעולה, ללא פנים, טקסט או ממשק מצויר.
- נוסף מיד אחריה שקף ״בוקר טוב״. צילום המרצה תופס 28% מרוחב הבמה במסגרת אנכית, והברכה והשם נשארים אובייקטים נפרדים ועריכים.
- שקף הסיום הומר מתמונת מסך מלא לקומפוזיציה דו־חלקית: צילום קטן, ״תודה״, שם, `cfir94@gmail.com` ו־`052-656-8094`. כל שורת קשר היא אובייקט נפרד לעריכה.

### גוון סגור אחד — 2026-09-11

הערה של המרצה: ״מפריע לי שאין פלטת צבעים סגורה אחת… לבן, שחור, סגול, וכחול/תכלת… כרגע במצגת יש יותר מידי אדום חום, ואפור ירוק. גוון רחב מידי לטעמי.״ הוא צירף שקף לבן שבו ״עד הסוף״ צבוע במעבר תכלת→כחול→סגול, וקבע אותו כרפרנס.

- **טבלת הפלטות ירדה מ־14 ל־6.** נשארו `pearl` (הבמה הבהירה — היא נושאת את הרמפה שהמרצה סימן), `prism` (אותה רמפה על דיו), `ice` (הבית), `cobalt` (הקרקע העמוקה), `violet` (הקצה הסגול, שקף אחד) ו־`steel` (שחור ולבן בלי גוון — שם משהו נשבר). הוסרו `rose`, `amber`, `danger`, `mint`, `sand`, `paper`, `mist` ו־`midnight`. **טבלת הערכות ירדה מ־7 ל־4**: הוסרו `wine`, `forest` ו־`ember`, והארבע שנשארו כוילו מחדש לאותן טוקנים.
- **כל ה־`spectrum` נחתכו מאותה רמפה.** גם מדרג ה־`token` צומצם מ־43° לטוקן ל־17°, כי הצעד הרחב הוסיף ורוד וכתום לשקף שאין לו עסק איתם, ובלוב ה־`mesh` הוורוד הוחלף בסגול של הרמפה.
- **אופציה שיוצאת מהטבלה לא נעלמת מהמסמכים.** `RETIRED_PALETTES` ו־`RETIRED_THEMES` ממפים כל שם שהוסר לאופציה שקיבלה את תפקידו, והאימות מתרגם אותו. בלי זה עותק שמור בדפדפן עם `amber` היה נכשל כמסמך פגום — כלומר המרצה היה מאבד את העבודה שלו בגלל צמצום טבלה. שם שמעולם לא היה קיים עדיין נכשל.
- **`data-tone` על השקף.** פלטה מצהירה `tone: "light" | "dark"` והבמה כותבת אותו כמאפיין. זה מסמן במפורש את השקפים שבהם **סמל כלי חייב להיות הגרסה השחורה** — ויש בדיקה שנכשלת אם סמל לבן מגיע לשקף בהיר.
- **הבאג שדווח.** שני שקפי `pearl` נשאו את סמל ChatGPT הלבן, שפשוט נעלם על רקע בהיר. שניהם הועברו לגרסה השחורה מהמניפסט.
- **הדגשה בכותרת.** `*מילה*` עבד עד היום רק בשורת ההקשר. עכשיו `headline` עובר דרך `rich` (למעט `cascade`, שצריך כל מילה באלמנט משלה), ו־`.emph` לוקח את ה־`spectrum` של השקף במקום צבע משלו — ההדגשה היא אותה מחווה בכל הדק.
- **הצבעים החמים שנשארו הם כרום של העורך בלבד** — כפתור מחיקה, שדה לא תקין, הודעת שגיאה. הם לא מגיעים לבמה שהקהל רואה, ואדום על כפתור מחיקה הוא ממשק נכון, לא דליפה מהפלטה.

### הרמפה מהדגימה של המרצה, וקשת טונלית — 2026-09-11

המרצה דגם בעצמו את שני הצבעים מהשקף שהוא אוהב, מליבת האותיות ולא מהשוליים: **סגול `#7a59bc`** ו**כחול `#4378ab`**. הגרסה הרוויה שנבנתה לפני כן (`#7c3aed`/`#3b82f6`) נדחתה — היא נקראה ניאון. שלוש הערות נוספות: המעברים משעממים (״סתם פייד אאוט״), הלבן מתחיל מאוחר מדי, והמעבר מכהה ללבן ״קופץ חריף נורא לעין״.

- **הרמפה.** עברית נקראת מימין לשמאל, ולכן הסגול הוא עצירת ה־98% והכחול עצירת ה־2%. שקף בהיר נושא את הרמפה כפי שנדגמה; שקף כהה נושא אותה מורמת בבהירות בלבד (`#7fb2d9 → #96a3dc → #ac94df`), אחרת שני הגוונים הולכים לבוץ מול המשטח. שש פלטות, שני גוונים — הן לא יכולות להיבדל בצבע, ולכן הן נבדלות ב**ערך**: לבן, דיו, פחם, נייבי, חציל, שחור. סולם הערכים הזה הוא גם מה שמאפשר לעבור בין בהיר לכהה בלי שהמעבר ינחת כסטירה.
- **קשת טונלית בבלוקים.** בהיר וכהה הם מערכות, לא רעש: פתיחה בלבן (שקף 1, לא שקף 7), שישה שקפים כהים ״בתוך המכונה״, `steel` היכן שנשבר, בלוק בהיר שלם לכל הפרק על הכלים האמיתיים — כולל שקף הרפרנס עצמו — שקף `violet` אחד לרגע האנושי, וסיום בלבן. שישה היפוכים טונליים בסך הכול, כל אחד על `cut`.
- **המעברים.** `fade` ירד מ־23 מתוך 36 ל־11. עכשיו: `push` 13 (רצפים), `fade` 11 (המשכיות), `cut` 9 (פנייה בטיעון), `zoom` 3 (נחיתה). גם התנועה: `rise` ירד מ־26 ל־14, ו־`wipe`, `zoom`, `still` ו־`cascade` נכנסו לשימוש לפי משמעות. הכלל שנוסח ב־`AGENTS.md`: אופציה שיושבת על יותר משליש מהשקפים היא המונוטוניות הבאה.
- **`neural` נכנס סוף־סוף לשימוש.** הרקע שהמרצה ביקש בשמו לא הופיע באף שקף. הוא יושב עכשיו על שני השקפים שמדברים על המודל עצמו, `halo` על הרגע האנושי, ו־30 מתוך 36 שקפים נשארו `plain`.
- **שני באגים שהתגלו בבדיקה בדפדפן.** (א) אובייקטים חופשיים נשאו צבעי חומר שנשארו מפלטות שהוסרו — ענבר `#f0bf7b` וירוק מנטה `#96e3c0` — בדיוק ה״אדום חום ואפור ירוק״ שהמרצה התלונן עליו. (ב) 24 אובייקטי טקסט נשאו `#f6f7f8` קשיח, שנעלם לגמרי ברגע שהשקף הפך לבהיר. כולם הוחזרו ל־`auto`. נוספה בדיקת ניגודיות בדפדפן שעוברת על כל 36 השקפים.
- **`*מילה*` עובד עכשיו גם באובייקט חופשי**, לא רק בשדה המובנה — אותה מחרוזת בדיוק הודגשה במקום אחד ונקראה כטקסט גולמי במקום השני. בזמן עריכה בפועל הקופסה נשארת טקסט רגיל, כדי שהמרצה יערוך את מה שהקליד ולא את הסימון.
- **אובייקט `spectrum` על `auto`** לוקח את הרמפה של השקף במקום גרדיאנט שנגזר מצבע האובייקט. גזירה מצבע נשארה למי שבחר צבע בכוונה.
- **תמונת המפתח של הפתיחה הוסרה מהבמה** (הקובץ נשאר במסמך). היא הייתה ענבר וירוק — מחוץ לגמוט — וכהה, וזו הייתה אחת הסיבות שהלבן ״קפץ״ אחר כך. השקף ממתין לתמונה שהמרצה ביקש לשים שם.

### ארבעה באגים מהבמה, ובדיקה שתתפוס את הסוג הזה — 2026-09-11

המרצה דיווח מהמצגת החיה: התמונה בשקף הראשון נעלמה, הרקע ״ניהיה כחול״, ״משפטים מבולגנים״, ולוגואים לבנים על רקע לבן. כולם היו אמיתיים, ולשלושה מהם היה שורש משותף — דברים שקורים בזמן המעבר, שבדיקות הסכימה לא רואות.

- **הקרקע מאחורי הבמה עוקבת אחרי השקף.** `.theater` ו־`#stage` השתמשו ב־`var(--surface)` שנפתר מול ערכת המצגת, כי הטוקנים של הפלטה יושבים על `.slide` — בתוך הבמה. התוצאה: כל שקף `pearl` היה מלבן לבן שצף במסגרת נייבי. `render` כותב עכשיו את הפלטה של השקף הנוכחי גם עליהן, ומתאים את מהירות המעבר של הקרקע למעבר עצמו (`cut` = מיידי). לשקף `deck` נכתב כלום, כך שנפילת החזרה לערכה נשמרת.
- **השקף היוצא נשאר למטה ונשאר אטום.** הוא ישב מעל ב־`z-index: 3` והתמוסס, ולכן שליש שנייה הקהל קרא את הכותרת הנכנסת דרך היוצאת — שני משפטים זה על זה. זה היה ה״משפטים מבולגנים״. השקף הנכנס הוא זה שזז עכשיו; הוא מכסה.
- **טרנספורם על צאצא שובר `background-clip: text` של אב.** המילה עוברת קומפוזיציה בשכבה משלה והגרדיאנט לא מגיע אליה. כותרת עם רמפה שפוצלה למילות `cascade` לא נרשמה עמומה — היא לא נרשמה בכלל, כי המילוי שקוף ואין מה שצובע אותו. כל מילת `cascade` נושאת עכשיו את הרמפה בעצמה.
- **`headline` סופר מילים לאורך כל המשפט.** כותרת והדגשה הן שתי קריאות; בלי היסט שתיהן התחילו ממילה אפס והמשפט הגיע בשני גלים חופפים.
- **ארבעה סמלים לבנים על שקפים בהירים.** הבדיקה הקודמת בדקה רק את `slide.mark`; אלה היו אובייקטי תמונה חופשיים, ולכן חמקו. `tools/reink-marks.mjs` צובע מחדש כל סמל בעל דיו שטוח לצבע שהשקף שלו מבקש, משמר אלפא והחלקת קצוות, ומסרב לכל מה שאינו מונוכרומטי — כך שתצלום לא ייפגע.
- **תמונת הפתיחה הוחזרה.** הסרתי אותה כי היא ענבר וירוק וכהה; המרצה דיווח על היעדרה כבאג. היא חוזרת עד שתגיע התמונה שהוא בחר לשקף הזה.
- **`tools/stage-audit.mjs`** — הולך על המצגת הבנויה בדפדפן אמיתי ונכשל על טקסט לא קריא, גרדיאנט שלא נצבע, תמונה שלא נטענה, מסגרת שלא תואמת לשקף, ושגיאות קונסולה. זה מכסה חלק מהפער שנרשם ב־`TASKS.md` — בדיקות הסכימה לא רואות את הבמה, וכל באג שהמרצה דיווח עליו עד היום חי שם.

### מעברים שמכסים, ותשע הערות מהבמה — 2026-09-11

המרצה צילם באג בווידאו: ״כשאני עובר שקף השקף הנוכחי קופץ, נעלם, ואז רואים אותו מימין זז ימינה ואז מופיע השקף הבא.״

- **תיבת השקף הנכנס לא זזה, לא מתקרבת ולא נעלמת — רק מה שמצויר עליה.** `t-push-in` התחיל את התיבה ב־9% שמאלה, ולכן רצועה של 119 פיקסלים בצד ימין לא כוסתה כלל — והשקף היוצא נסחף לתוכה במלוא הראות. נמדד לפני (119px חשופים) ואחרי (0 בכל פריים, בשני הכיוונים, בשני רוחבי מסך). כל מעבר שזז מנפיש עכשיו את `.atmosphere`, `.scene` ו־`.free-object-layer`; התיבה מכסה מהפריים הראשון. אותו באג בדיוק היה ב־`zoom` (scale 0.94 = מסגרת חשופה מסביב).
- **שישה מעברים במקום ארבעה, וכולם איטיים יותר.** `dissolve` (המסה רכה, 0.86s) ו־`recede` (זום החוצה) נוספו; המשכים עלו מ־0.36–0.44s ל־0.62–0.86s. אף אופציה לא עוברת שליש מהדק: push 11, cut 9, dissolve 6, fade 4, zoom 3, recede 3.
- **`gather` הוא המהלך היחיד שנועד להיקרא כמהלך, והוא שמור.** סמל ג׳מיני נוסע ממקומו בשקף הקודם למרכז, ורק אז נוטבוקLM מגיע. שקף 16 החזיק **ציור אחר** (הוורדמארק המלא, יחס אחר) ולכן זה נראה כקפיצה; עכשיו זה אותו סמל בדיוק, באותו גודל, ו־`object-gather-in` מתחיל 26cqw משמאל למרכז — בדיוק היכן שהוא עמד. נמדד: cx=377 בשני השקפים ברגע החילוף, זהה בציור ובגודל, ואז נסיעה של 1.25s ל־cx=720. השקף הקודם לא נותן לסמל יציאה, אחרת הוא נעלם לפני החילוף.
- **שלושת שקפי הכלים קיבלו אופי משלהם** (`rise`/`zoom`/`fade` לסמל, `fade`/`rise`/`wipe` למילים) ואף אחד מהם אינו `gather` או `curtain` — מהלך חתימה שנשרף פעמיים אינו חתימה.
- **`icon` אופציונלי לכל צד ב־`split`.** שקף 21 קיבל את שני האייקונים שהמרצה שלח, ושקף 18 את בועת השיחה. האייקונים צוירו ב־`tools/make-glyphs.mjs` כנתיבים מקווקווים בדיו שטוח אחד, ולכן `reink-marks.mjs` יודע לצבוע אותם — והוא הורחב לראות גם `sides[].icon`, לא רק אובייקטים חופשיים.
- **שקף 18 לא אמר את מה שהוא בא לומר.** הצד ״מעבר לשיחה״ תיאר דרישה ולא גבול; הוא אומר עכשיו ״כאן הוא נעצר.״ **זו מילה של המרצה ששונתה — לאישורו.**
- **שקפי הראיות היו תמונות קטנות על שחור שטוח.** ה־inset העליון היה 23% מבמה שלא צויר עליה דבר (הכיתוב יושב למטה); הוא ירד ל־7%, ו־`mesh` נכנס מאחור במקום `plain`. שקפים 26–28 איבדו כותרת וכיתוב לגמרי לבקשת המרצה — בלי טקסט אין `has-text`, והתמונה מקבלת כמעט את כל המסגרת. שקף ״התשובות״ הוסר.

**חסום:** תמונת בועת הצ׳אט לשקף 18 הגיעה כתמונה בצ׳אט ולא כקובץ, ולכן אי אפשר להטמיע אותה.

### משפחת הקשתות, ובקרת קצב — 2026-09-11

המרצה אוהב את `arcs` וביקש עוד גרסאות ״יותר נוכחות ויותר דינמיות״, וגם אפשרות לבחור את אורך אנימציות הכניסה והיציאה.

- **שלוש גרסאות חדשות, `arcs` המקורי לא נגע.** `orbit` (שלוש טבעות שמסתובבות ברציפות במהירויות שלא מתיישרות), `comet` (הקשת עומדת והאור נוסע לאורכה — דורש `@property --sweep` כדי שזווית ה־conic תוכל להיות מונפשת) ו־`ribbons` (רצועות רכות עם גוף במקום קו שיער).
- **שני דברים שהיו חייבים להשתנות מול המקור.** הראשון: `arcs` ממלא את פנים המעגל ב־`var(--surface)` דרך `padding-box`. על פלטה שטוחה זה בלתי נראה, אבל על `prism`/`cobalt`/`violet` — שנושאות עכשיו גרדיאנט — זה דיסק שטוח שמכסה את הרקע בקצה חד. השלושה החדשים ממוסכים לטבעת, והפנים שקוף. השני: הם לא משתמשים ב־`.light-arc`, כי `.no-motion` מנקה אותו — האנימציה שלו היא כניסה חד־פעמית, ולולאה סביבתית לא אמורה להיעצר כי המרצה התקדם צעד.
- **`ribbons` נשמר מחוץ למרכז הפריים.** הגרסה הראשונה הייתה רצועות רחבות ומטושטשות שחצו את המסך — הן הפסיקו להיות אור מאחורי המילים והפכו לערפל לפניהן. עכשיו שתיים למעלה ושתיים למטה.
- **`pace` — שדה חדש לכל שקף.** בוחר כמה זמן רצות האנימציות שלו: המעבר, והכניסה והיציאה של כל אובייקט חופשי, כולל `gather`. נמדד: `fade` 0.216s / 0.36s / 0.522s / 0.72s לארבעת הקצבים, ואובייקט 0.432s→1.44s. `natural` ראשון בטבלה ושווה בדיוק 1, ולכן מסמך שקדם לשדה לא משתנה. שקף יוצא שומר על הקצב של עצמו — זה עדיין הוא שיוצא.
- **באג שהוכנס בסבב הקודם ונתפס עכשיו.** ל־`app.js` הייתה טבלת זמנים פרטית `{cut, fade, push, zoom}`. ברגע ש־`dissolve` ו־`recede` נוספו היא החזירה `undefined`, הטיימר להסרת השקף היוצא הפך ל־`NaN`, והשקף היוצא נעלם בפריים הראשון. עכשיו `TRANSITION_MS` ב־`core.js` הוא המקור היחיד — גם ל־CSS דרך `--arrive-base` וגם ל־JS — ויש בדיקה שנכשלת אם למעבר אין משך. נוספה גם בדיקה שכל רקע בטבלה מצייר מחלקות שקיימות בגיליון.

### דרך העבודה בין העותק בדפדפן לריפו — 2026-09-11

המרצה שאל אם אפשר לייצא את השינויים הידניים שלו ולהחיל אותם על הפרויקט, ואם זו דרך עבודה נכונה.

- **אפשר, וה־JSON הוא הפורמט.** נבדק: `validate()` על `src/content.json` מחזיר בדיוק את מה שכפתור ״קובץ תוכן״ מייצר — ההבדל היחיד הוא מילוי ברירות מחדל לשדות שהקובץ בריפו משמיט (כרגע `pace`). כלומר הייצוא נופל ישירות על `src/content.json`, וה־build חותם `revision` מחדש מה־hash של התוכן.
- **`tools/apply-export.mjs`** קורא ייצוא כזה, מדפיס כל שינוי במילים (שקפים שנוספו/הוסרו/סודרו מחדש, כל שדה שזז, וכל אובייקט חופשי שזז — לפי id), ורק עם `--write` מחיל. מסמך שנכשל באימות נדחה במלואו ולא מוחל חלקית. נבדק על עריכות מלאכותיות: זיהה הסרת שקף, סידור מחדש, שינוי רקע וקצב, שינוי כותרת, והזזת אובייקט ב־x/y/entrance/opacity.
- **זו החלפה מלאה, לא מיזוג.** הצד שמוחל מנצח לגמרי. לכן הדיף מודפס קודם, ולכן הפרוטוקול הוא ששני הצדדים לא מחזיקים שינויים לא־שמורים בו־זמנית: המרצה מייצא ושולח **לפני** שהוא מבקש עבודה חדשה, ולוקח את הגרסה החדשה אחרי כל פרסום.

### שקפי הסיום, ובאג שחסם את העריכה — 2026-09-11

- **העורך נפתח ריק, וזו הייתה רגרסיה שלי.** `pace` נוסף לסכימה ולשורת המראה אבל לא ל־`LOOK_KEYS`, ולכן `look.pace` חזר `undefined`, `fieldsFor` נפל עליו (`Cannot read properties of undefined (reading 'link')`), וכל פאנל השקפים נרשם ריק. השורש היה שתי רשימות של אותם שדות; עכשיו `LOOK_KEYS` היא היחידה והשורה נבנית ממה שנוחת ב־`look`. `tools/stage-audit.mjs` פותח עכשיו את שני הפאנלים — נבדק ששבירה מכוונת אכן נכשלת שם.
- **שלוש הודעות אחסון שהיו אחת.** הקוד בהתחלה תפס גם ״הדפדפן חוסם שמירה״ וגם ״העותק השמור אינו תקין״ באותו `catch` והציג ״השמירה אינה זמינה״ — בדיוק מה ש־`AGENTS.md` אוסר. עכשיו כל אחת אומרת מה קרה, והודעת המכסה נוקבת בגודל: המסמך שוקל 1.9 מ״ב, כלומר **3.8 מ״ב ב־localStorage** (UTF-16) מתוך ~5 מ״ב — טיוטה אחת שמורה מפוצצת את המכסה. זה כנראה מה שהמרצה ראה.
- **שלושה שקפי סיום חדשים** לפי מה שהמרצה ניסח: המלצה (`split` בשלושה צדדים עם האייקונים, כותרת גדולה כאובייקט חופשי מעל שורת המשנה), שאלה לעתיד (`statement` ב־`violet`), וסיכום עם ציטוט האסביס (`canvas`, כי המשפט הוא 67 תווים ואין שדה כותרת שמכיל אותו — ומילים של המרצה לא מקצרים כדי להיכנס למגבלה). השקפים שהם מחליפים הוסתרו, לא נמחקו.
- **שלושה באגים שנתפסו בבדיקה בדפדפן.** (א) כותרת הסיכום נחתכה — 219px בקופסה של 193px; הקופסה הוגדלה, והאודיט בודק עכשיו `scrollHeight` מול הקופסה בכל אובייקט. (ב) הציטוט באנגלית קיבל את המירכאות בצד הלא נכון, כי פסקה RTL מחליטה היכן נוחת תו ניטרלי; `dir="auto"` פותר את זה לשתי השפות בלי להצהיר על אף אחת. (ג) כותרות `split` היו בגודל קבוע, ובשלושה טורים ״סוכני AI״ נשבר לשתי שורות; הגודל מתחלק עכשיו ב־`--sides`, וכל שקף דו־צדדי נשאר בדיוק כפי שהיה.
- **`violet` מופיע עכשיו פעמיים**, באותו תפקיד: היכן שהשאלה על מה נשאר אנושי נשאלת, והיכן שהיא נמסרת לקהל בסיום. שתיים זה קישור; שלוש זה הרגל.

### תנועה בשכבות ורקע רציף — 2026-09-11

- **מעבר הבמה וכניסת התוכן נשארים שני מאפיינים שונים, אבל מרוכזים באזור אחד בעורך.** המעבר מתאר את החלפת הבמה; הכניסה מתארת את הופעת התוכן. בעבר שינוי `motion` כפה רינדור כאילו הגיע שקף חדש, ולכן גם `transition` התנגן בתצוגה המקדימה והסתיר את הבחירה. תצוגת המקדימה מפעילה עכשיו רק את כניסת התוכן.
- **CSS המעבר אינו נוגע עוד ב־`.scene` או ב־`.free-object-layer`.** הוא מוגבל ל־`.atmosphere`, כך שכניסת כותרת או אובייקט נשארת עצמאית. יציאות של אובייקטים מוצגות בשכבה שקופה מעל הבמה הנכנסת עד שהן מסתיימות.
- **אותו רקע הוא אותו רכיב DOM.** אם שני שקפים עוקבים חולקים `backdrop` ואותה תמונת רקע, הרקע הישן מועבר לשקף החדש במקום להיבנות מחדש. לולאות סביבתיות וקשתות נשארות באותו פריים ואינן מהבהבות או מתחילות מהתחלה.
- **אפשרויות האובייקטים הורחבו למשפחת התנועה המלאה.** נוספו המסה רכה, התבהרות, החלקה, זום בשני הכיוונים ומילה אחרי מילה לכניסה וליציאה, לצד מהלכי החתימה הקיימים.

### תצוגה מקדימה חיה לתנועה — 2026-09-11

- בחירת מעבר או כניסת תוכן בכרטיס של שקף מעבירה את הבמה מיד לאותו שקף ומפעילה את הבחירה. תצוגת מעבר משתיקה את כניסת התוכן, ותצוגת כניסת תוכן אינה מפעילה מעבר במה.
- כניסת תוכן חלה גם על אובייקטים חופשיים. טקסט חופשי תומך ב״מילה אחרי מילה״; תמונה, צורה או רכיב באותה בחירה מקבלים הופעה עדינה. לאחר ההחלה אפשר לכוונן כל אובייקט בנפרד.
- אומת בדפדפן על שקף השאלה: שבע מילים קיבלו אנימציית רצף ושני אובייקטי הטקסט נשאו `object-enter-cascade`; בחירת ״המסה רכה״ הפעילה `t-dissolve-in` על הרקע ו־`none` על האובייקטים. הקונסולה נקייה. הבנייה וכל 44 הבדיקות עברו.
### שליטה בתנועה מהסרגל, ו״מילה אחר מילה״ שלא עשה כלום — 2026-09-11

- **״מילה אחר מילה״ שיבש את הטקסט, ועכשיו לא.** הפיצול למילים פוצל את מחרוזת המקור וחיבר אותה בחזרה ברווח אחד. אובייקט טקסט הוא `white-space: pre-wrap`, כלומר הוא שומר את שבירות השורה שהמרצה הקליד — וחיבור ברווח אחד שיטח קופסה של שלוש שורות לשורה אחת רצופה. זה ה״חרבוש״. בנוסף: רווח כפול או שורה ריקה ייצרו span של מילה שאינה קיימת, והדגשה שנפרסת על שתי מילים (`*שתי מילים*`) איבדה את זוג הכוכביות כי כל מילה בורחה בנפרד. `cascadeWords` מפצל עכשיו את ה־HTML המרונדר ולא את מחרוזת המקור: עובר מעל תגיות, שומר כל רצף רווח כמו שהוא, ועוטף רק רצפי תווים נראים. אותה פונקציה משרתת גם כותרת וגם אובייקט.
- **פיצול המילים באובייקט נקבע לפי הכניסה של האובייקט עצמו**, לא לפי תנועת השקף — זה המודל שנבנה ב־main, והוא הנכון: מי שקבע לקופסה אחת ״מילה אחרי מילה״ מקבל את זה בקופסה הזאת בלבד. בזמן עריכה הטקסט נשאר מחרוזת אחת; פיצול מתחת לסמן הוא הדרך לאבד את הסמן.
- **בחירת תנועת שקף לא מוחקת עוד מהלך חתימה.** ההחלה על האובייקטים דורסת את `entrance` של כולם; `gather` ו־`curtain` פטורים ממנה. `gather` נושא את הסמל של Gemini מהמקום שבו עמד בשקף הקודם — נגיעה בתפריט התנועה הייתה מוחקת אותו בשקט.
- **מחלקת התנועה נמחקה בכל רינדור, לא רק בצעד.** רינדור שלא שינה צעד (פאנל שנפתח, שדה שנערך) מוחזק דומם על ידי `.no-motion`; מחיקת המחלקה גם שם השאירה את האלמנט בלי יכולת לתאר את השקף של עצמו. זה חלק מ״התצוגה מקדימה לא תמיד עובדת״. הכלל המלא היום הוא שלוש שאלות ולא אחת: שקף אחר — שומרים ומנגנים; צעד חדש באותו שקף — מוחקים; רינדור בלי שינוי — שומרים ומחזיקים דומם; ותצוגה מקדימה של תנועה — שומרים ומנגנים, אחרת הרינדור שאמור להריץ את הבחירה מוחק בדיוק את המחלקה ששמה אותה.
- **כפתור ״תנועה״ בסרגל הצף.** שתי הבחירות שהמרצה משנה הכי הרבה היו שלוש לחיצות עומק בתוך פאנל צד. הדיאלוג נבנה מ־`COMMON_FIELDS`, ולכן שדה שנוסף שם מופיע בו בלי להיכתב שוב, ויש בו כפתור הרצה חוזרת מפורש. נמדד: חמישה שינויי תנועה ברצף — כל אחד מחזיר `entering` ואת האנימציה הנכונה.
- **שמונה שדות הציגו את המפתח האנגלי שלהם** בפאנל עברי (`pace`, `layout`, `scale`, `arrangement`, `backdropPicture`, `mark`, `icon`, `minutes`). כל אחד נוסף לסכימה בלי התווית שלו. נוספה בדיקה שנכשלת על שדה בלי תווית.
- **סריקת באגים.** הורצה מול כל הטבלאות: 6 מעברים, 6 תנועות, 4 קצבים, 17 רקעים — כל אופציה הופעלה על שקף חי דרך האפליקציה עצמה; 57 עורכי שקפים נפתחו (המינימום 12 שדות בכל אחד); בטל/בצע שוב שש פעמים לכל כיוון. אפס שגיאות קונסולה. בנוסף נבדק סטטית: אין `$("#id")` ללא אלמנט, אין `data-action` שלא מטופל (`chat`/`agent` עוברים דרך `core.transition` ונבלעים בבטחה בשקף שאינו `experiment`), ושש מחלקות ללא CSS נבדקו ונמצאו סמנטיות בלבד — `.object-handle` מעצב את ידית השינוי, ו־`resize-se` הוא רק שם.
- **שתי דרכים לאותן שתי בחירות, ושתי התנהגויות שונות.** הסרגל הצף והפאנל הצדדי מימשו כל אחד את ״מעבר הבמה״ ו״כניסת התוכן״ בעצמו: הפאנל הציג כל דבר לחוד (מעבר בלי תנועת תוכן, תנועה בלי מעבר), והסרגל פשוט הריץ הכול מחדש יחד. זה בדיוק אופן הכשל שהמסמך הזה מתעד שוב ושוב — שני עותקים של אותו כלל שנפרדים. עכשיו `previewLookChange` היא הדלת היחידה, ושניהם עוברים דרכה. נמדד: שישה מעברים ברצף מהסרגל — כל אחד מחזיר `entering`, את המאפיין הנכון, ותוכן מוחזק דומם.
- **שקפי הסיום נבדקו מול מה שהמרצה הכתיב** — שלושתם קיימים ובסדר הנכון: המלצה (ובה שלושת החלקים, כולל המנוי), השאלה לעתיד, והסיכום עם ציטוט האסביס והייחוס המלא. תוקן דבר אחד: המעבר לשקף ״תודה.״ היה `dissolve`, והוא הפיכת גוון (כהה→בהיר). כל הפיכת גוון בדק נוחתת על `cut` — זה מה שגורם לה להיקרא כפרק חדש ולא כתקלה; שתי ההפיכות האחרות בבלוק הסיום כבר היו `cut`.
- **`backdropStrength`** — אחוז לכל שקף. מתחת ל־100 כל האטמוספרה דוהה; מעליו משפחת הקשתות מחזקת את האלפא והטבעת שלה, כי שכבה שכבר באטימות מלאה לא נעשית נוכחת יותר על ידי העלאת אטימות. נוסף גם רקע `corner` — קשתות עדינות בפינה הימנית התחתונה בלבד.
